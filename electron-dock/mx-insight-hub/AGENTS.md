# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## MX Insight Hub product context

- Treat `/Users/qpjoy/workspace/qpjoy/de/de-llaaddddeerr/electron-dock/mx-launcher/demos/ui-design-neon-void` and `mx-launcher/ui-design` as the visual source of truth.
- Hub defaults new browser profiles to the cool light Neon Void theme and keeps the existing dark theme as a persisted user-selectable mode. Theme changes are presentation-only and must not change authentication, routing, or API behavior.
- Use the shared searchable `DropdownField` for every popup value selector in the Hub. Do not use native `<select>` for Hub product controls because its expanded menu cannot be themed consistently; region-picker dialogs and navigation accordions remain their own interaction patterns.
- For the governed source catalog, the real Hub `DashboardPage` command-center layout is the large-screen visual baseline. The existing external-source pipeline page is an execution-semantic reference only, not the dashboard reference.
- Keep `/source-catalog` as the governed business directory. Present source execution under the first-level “数据清洗中心”, with “数据库配置” and “清洗任务计划” as separate second-level surfaces; do not merge catalog status/storage with runtime source status/storage.
- Present live paid upstream API integrations under the same first-level “数据清洗中心” in the separate second-level surface “外部数据平台”. This surface is a provider command center, not a scheduled cleaning-plan list: it must distinguish Hub requests from actual upstream dispatches, successful upstream envelopes from usable canonical results, and idempotent replay, fresh cache, stored fallback, rejection and unknown outcome. A provider connector is optional and must never become a Launcher identity dependency, a Hub readiness dependency, or a reason for existing stored reads and admin/login paths to fail.
- Keep public data contracts provider-neutral even when one provider currently supplies them. The authorization platform and canonical dataset describe the Hub business product; the physical provider belongs in versioned adapter evidence, immutable response archives and canonical lineage. A caller-supplied `Idempotency-Key` identifies one exact request (including its page/cursor), so changing pages requires a new key; generated keys may provide bounded exact-request deduplication for clients that omit one. Never blindly retry an ambiguous paid dispatch, and preserve the distinction between an upstream `code=0` billed call and a response the Hub could successfully normalize.
- Database configuration profiles are optional transport/credential reuse only. A cleaning source/plan still owns its table locator, cursor, mapping, schedule and evidence; complete inline configuration remains valid, and fixed SQLite/custom-HTTP adapters are not forced into a database profile.
- Treat mobile multi-commerce collection as authorization platform `mobile_commerce`, with 快手/抖音/淘宝/闲鱼 retained as stable source-catalog/classification facets. Catalog related-data and public catalog-item queries must use the reviewed catalog-entry ID, not compare marketplace names to the authorization platform. Its first public capability is stored-only; rows follow canonical outbox → Elasticsearch projection/search. Any future remote refresh is a separately granted asynchronous command executed by an external mobile collector, while Hub owns only trigger/status, cleaning, indexing and data APIs; it is never an implicit public-read side effect.
- Present the Hub-owned virtual supermarket as one data product with three truthful modes: “逛超市” for guided shelf browsing, “超市全景” for a spatial renderer and “目录模式” for dense accessible management/browsing. The panorama is presentation only: public contracts expose stable department/aisle/shelf/position semantics, never WebGL coordinates, camera, lighting, meshes or materials. `virtual_supermarket` is an explicit Public API platform grant separate from `mobile_commerce` and `source_catalog`; its public reads are on-shelf-only safe projections. Off-shelf/archive changes the storefront publication record without deleting the referenced canonical capture. Preserve `storefrontRevision` in metadata/pages and bind opaque cursors to the complete normalized filters, sort, page size and revision. Mobile-commerce captures remain the immutable source observations; Agent Studio may propose category, specification or identity mappings with evidence, but cannot publish, merge, delete or move products automatically.
- Present the JustOne-backed search experience as the Hub-owned data product “电商数据百宝箱”. Public docs and callers keep the provider-neutral `POST /api/v1/data/ecommerce/products/search` contract; Admin-only UI may disclose JustOne as the current adapter, lineage and capability atlas. The product defaults to a clearly labeled zero-upstream-call demo, keeps any Hub consumer key in component memory only, requires explicit confirmation before a possibly billable live request, and reports `live`, `fresh_cache`, `stored_fallback` and `idempotent_replay` without guessing cost from latency. In the source catalog, mark only 淘宝、天猫、京东、小红书店铺、闲鱼 as `JustOne · 商品搜索已接`; leave 抖音电商、快手小店 as `JustOne · 接入线索` until a runtime contract is verified.
- The fixed mobile-commerce v1 dataset worker reads one active PostgreSQL source-catalog snapshot per batch and fails closed on exact-column drift, malformed/non-finite checkpoints or rows, actual NULL writer-required values, or loss of the unique `id` identity index. Writer v3 admits `timestamp`/`timestamptz` plus a narrow mobile-only `text`/`varchar` watermark: textual values must be exactly 19 characters in `YYYY-MM-DD HH:mm:ss`, are ordered with PostgreSQL `COLLATE "C"`, and are interpreted as Asia/Shanghai before canonical UTC normalization; generic database sources remain unchanged. A timestamp `(collected_at,id)` index or textual `((collected_at COLLATE "C"),id)` index is a reported performance recommendation rather than a correctness gate when unique `id` already proves total order. Nullable DDL for writer-required fields is likewise an explicit warning governed by the current Writer attestation and a same-statement runtime NULL/format guard. `dsnEnv`, ID reuse, table replacement and public share/open-app tokens are outside this fixed contract.
- Treat `syncIntervalSeconds` as online scheduling policy for every cleaning plan: an interval-only save may run while active/running/draining, leaves current and queued work unchanged, and is reread on the next scheduler scan. Connection, locator, mapping and checkpoint-contract changes still require the existing pause/drain/probe gates.
- Keep `GET /api/v1/data/canonical/items/{id}/timeline` as the formal public bidirectional Telegram reading contract. The first call accepts independent `before`/`after` windows and returns ascending items plus `anchorIndex`; continuation calls return exactly one opaque HMAC cursor whose signed payload owns the older/newer direction and binds contract version, authorized dataset/chat stream, exclusive `(eventTime, canonicalId)` boundary, page size and authorization scope. Search cursors and timeline cursors are separate domains. Timeline currently supports only the two registered Telegram message datasets, requires the `telegram` grant, reads Hub stored data without invoking Telegram/upstream collectors, and uses live-keyset rather than frozen-snapshot semantics; it does not claim upstream completeness or a changes feed.
- Keep `mobile-commerce-data-processing` in Agent Studio as a compile-only, human-reviewed mapping-proposal template that captures source-specific identity, timezone, privacy, lineage and drift lessons. The fixed deterministic mapping remains the production path until an evaluated Agent artifact is explicitly approved; Studio must never imply that the template imports, publishes or remotely collects data.
- Data-cleaning navigation, shared database configuration and mobile-commerce ingestion are Hub-only changes. They must not alter MX Launcher/MX-H2I login, Domestic/Internal routing, WireGuard, DNS or existing user connectivity.
- Source-catalog metadata is authoritative in PostgreSQL. Elasticsearch may later receive a rebuildable outbox projection, but application requests must never dual-write PG and ES.
- Source-catalog status has four independent delivered axes: coverage (`unknown/not_covered/partial/covered`), delivery (`exploring/planned/doing/blocked/complete/paused/retired`), field review, and runtime health. A `complete` delivery may still have unknown or degraded runtime health.
- Use the supplied Sub2API dashboard screenshot only as the information-architecture reference for accounts, API keys, plans, quotas, usage, channels, and operational metrics. Do not copy its branding.
- The primary workflow is: create a consumer, issue a one-time API key, grant explicit platform capabilities and limits, call Hub-owned APIs, and inspect usage/latency/error evidence. Migrated operations dispatch through Hub-native provider connectors; only explicitly documented, not-yet-migrated compatibility shapes may still use the historical Night-All path.
- Night-All remains an internal data source. Never expose its provider credentials, provider names, endpoint IDs, `businessId`, or `availabilityMode` to public clients.
- Paid upstream credentials and provider-management details stay out of tenant/Public and ordinary Admin UI. The Admin-token-only external-platform page may manage them, and may reveal a database-managed key only after an explicit second Admin Token check; no tenant session, Launcher session or Public API key may receive them.
- “Redaction” must never mean truncating, filtering or rewriting acquired business data. Preserve complete upstream business payloads (content, author/profile fields, tags, metrics, media, signed media URLs and pagination) in the restricted raw archive, alongside PG canonical/revision data and rebuildable ES projections. Provider-shaped, authenticated compatibility APIs preserve the upstream business envelope; Hub-owned product APIs may expose a stable documented projection. Only the Hub-to-provider request credential itself (API keys and Authorization/Cookie material) stays out of responses, ordinary UI and logs.
- Apply that provider-identity boundary to source-catalog presentation as well: raw Admin-token records may retain internal connector/lineage names, but tenant/Public projections and every visible UI path (including search, facets, CSV, detail and edit forms) must hide paid-provider identity without overwriting the authoritative record.
- Platform compatibility APIs may preserve familiar provider-style path names (for example `/api/v1/xiaohongshu/app/get_note_info`) when that reduces caller migration cost. These are still Hub-owned contracts: Hub tenant identity, Live API keys, grants, quotas, billing, idempotency, stable response projection, cache and canonical/archive lineage remain authoritative; keep the provider-neutral Hub endpoint in parallel and never expose the physical upstream or its credentials.
- Keep the three historical `/api/v1/search/raw|crawl|user-info` spellings as exact aliases of the Hub compatibility routes. Xiaohongshu variants that have a verified Hub-native TikHub contract must dispatch inside Hub rather than through Night-All; preserve the historical response envelope and one paid-operation fingerprint across aliases. Bound every explicit page and opaque traversal to at most 15 pages. For any still-historical Night-All traversal, expose only the Hub-encrypted `mxnc1` continuation bound to consumer, operation, platform, stable query scope and next page; clear continuation on page 15, reject pre-wrapper provider cursors/continuation params with `400 invalid_cursor`, and require restart from page 1 without a cursor plus a new `Idempotency-Key`. Use a new key for every next-page business request. This pagination-control rewrite is the only compatibility response governance mutation: never filter, redact or truncate business fields, long bodies, `raw_info`, `raw_data` or correlation values. The historical hop retains the complete parsed JSON payload and legacy raw strings; only Hub-native provider calls additionally promise exact upstream response text/bytes plus hash in restricted raw storage. The Xiaohongshu historical fallback of `/api/v1/data/search` uses the same `mxnc1`/15-page rule while existing `mxec2` direct Xiaohongshu cursors remain pinned to the Hub-native connector. Search previews at the known 60-character boundary are incomplete until a same-note detail result supplies strictly longer text; retain partial-completeness evidence when repair cannot finish.
- Record reviewed upstream procurement cost before every paid dispatch and keep provider/customer amounts in their original currencies. The monthly procurement/subsidy thresholds are hard admission limits only for requests without their own positive `enforced` per-request wallet hold; a correctly scoped, funded downstream charge must not be rejected merely for crossing those Hub financial thresholds, even when the two ledgers use different currencies. The exact current request still needs valid cost evidence, while provider rate/quota, concurrency, circuit breaker, contract, credential, idempotency and pagination protections remain authoritative. Downstream prices remain operator-entered immutable plan versions; never invent a customer rate, currency conversion or margin assumption in code or UI.
- For Xiaohongshu link input on that platform-shaped path, prefer JSON POST so temporary share parameters do not enter the request target, proxy logs or browser history. Keep GET `note_id`/`share_text` only as a compatibility surface, recommend `note_id` on GET, and keep delivery mode inside the exact idempotency fingerprint while sharing note identity for snapshots and paid-dispatch suppression.
- MX Launcher owns deployment and human-operator access. MX Insight Hub owns tenants, consumers, API keys, grants, plans, credits, usage, request idempotency, and billing evidence.
- Agent Provider catalogs must use page-level table/list CRUD with explicit create, edit, delete, test, and secret-reveal actions. Do not put whole-catalog Provider creation/editing back into a vertically stacked modal. Proxy endpoints and Proxy Sequences likewise keep visible CRUD on the page; Provider/Hub bindings are labeled as compatibility controls, while new business routing is selected on the LLM Sequence.
- Agent Market is a real, manageable catalog rather than a fixed demo gallery. Categories are operator-created records, Agents are persisted catalog records, and built-in examples must truthfully disclose whether they have an executable adapter and real run data. Seed a knowledge-Q&A catalog entry and place the existing advanced-search tutorial under a Demo category; never fabricate run counts, accuracy, latency, or health for display-only records. Custom Agents may be created and managed before an executor is attached, but the UI must label them non-runnable instead of simulating results.
- Keep the Agent Market stage inspector non-sticky on desktop, stretched with the workbench, and explicitly collapsible toward the right so the graph can reclaim space. Graph nodes, edges and loop decorations must remain clipped inside the middle workbench layer and never paint above the inspector. A zero-millisecond terminal Trace may be a real synchronous guardrail result; show its terminal status and reason instead of implying it did not run. Refresh continuity may use only bounded, redacted, Hub-admin-only browser-session history; it is not a server audit ledger. The longer-term Studio must separate authoring graphs, compiled executable artifacts and approved immutable deployments, with code-owned node/tool registries and no change to MX-H2I login or networking.
- Agent Center is the single first-level navigation branch for all model and Agent controls. LLM Provider, LLM Sequence, LLM Proxy, Agent Market, Agent Studio and the legacy runtime are its second-level items; Agent Studio must not appear as a separate first-level branch.
- Agent Studio opens on a portfolio that lists every Studio-owned Agent product and keeps Draft, compiled Artifact, Eval, Release, Deployment and Market visibility as separate truthful lifecycle facts. A concrete graph editor is a project Draft detail route, not the Studio landing page. P1 may enable governed Draft editing and server compilation only; unavailable Sandbox, Eval, Release, Deployment and Market publication steps must remain visibly disabled instead of being simulated.
- Studio LLM nodes reference an approved LLM Sequence. The effective Provider/model/Proxy route is resolved and shown as read-only evidence from that Sequence; Provider, Proxy and Sequence are execution configuration and must not be modeled as business graph nodes or request-level endpoint overrides.
- Source-catalog categories such as news, social media, enterprise, filing, entertainment, maternal/child, ecommerce, search, industry/commerce and legal are facets rather than mutually exclusive folders. A catalog entry is governance metadata, not a runnable connector: Studio and Data Center must not claim a source can analyze or import until a physical source, approved mapping and canonical dataset contract exist.
- Provider Catalog is an account directory, not a service default: Catalog order and the first record never imply a business default. Every business model call must resolve an explicit LLM Sequence, including the one-Provider case; Chat and Embedding may legitimately have no default. Direct Provider calls are limited to operator connectivity probes and rolling-migration compatibility.
- An Embedding Provider may explicitly inherit the connection, credential, timeout, and compatibility Proxy binding of one database-managed Chat Provider; it never copies the secret and never inherits the Chat model. The Embedding model and dimensions remain explicit. Known unsupported vendors/protocols are unavailable, known supported models use the maintained capability catalog, and unknown OpenAI-compatible gateways remain available only with a visible probe-required warning. Changing an inherited Chat connection invalidates the dependent Embedding revision and Sequence proof; no catalog entry is selected implicitly.
- LLM Proxy records are optional. Creating the first endpoint or Proxy Sequence never binds it. An LLM Sequence has an explicit three-state egress policy: inherit, Pod/Node system egress, or one ordered Proxy Sequence. Inherit resolves the compatibility Provider and Hub overrides first, then the deployment-observed Docker daemon proxy, and finally Pod/Node system egress when no daemon proxy applies. Show the observed daemon source and redacted effective values as read-only evidence; confirmation may unlock Hub-owned policy editing, but the UI must never edit or restart the host Docker/systemd service.
- Build Proxy Sequences by dragging only the dedicated endpoint/step handles, with insertion ordering and a drag-out removal target; cards, inputs, scrollbars and sliders must never be draggable. Keep arrow, add and remove buttons as keyboard/touch alternatives.
- Provider connectivity evidence is scoped to the exact egress route being verified. Creating or editing an unbound Proxy catalog entry must not invalidate a direct Provider test; changing an LLM Sequence's selected Proxy route, endpoint URL, endpoint state, order, or fallback policy must invalidate the Sequence proof. One-shot Provider × route tests require an explicit system/Proxy choice, never preselect the first record, never mutate bindings, and never write reusable probe evidence.
- Chat Provider and Sequence connectivity probes must leave enough output budget for reasoning-capable models to reach final `content`; reasoning-only output is never a successful Agent result and its chain-of-thought must not enter errors or logs.
- Records referenced by Agent control objects are protected by the server transaction, not only by disabled UI actions: a Provider must leave every LLM Sequence before deletion, a Proxy endpoint must leave every Proxy Sequence, and a Proxy Sequence must be unbound from every LLM Sequence plus the legacy Hub-global/Provider compatibility bindings.
- Reuse the shared Neon Void `qp-modal`/`qp-dialog` primitives and Hub `ConfirmDialog` for destructive browser actions. Do not add new `window.confirm` flows; dangerous dialogs initially focus Cancel and remain non-dismissible while busy.
- Server-file sources use a plain pasteable path input, never a directory/path dropdown. Treat the pasted absolute path as transient: runtime allowlists reduce it to `rootId + relativePath`, and catalog/log/response lineage must not persist the host path. “Archive by structure” means reuse an immutable format-rule version; never move or delete the source file.
- File registration may classify a value-free first/middle/last structural sample automatically. Known producers use a stable logical rule (for example `rule-twitter-canyie`) whose CSV/JSONL layouts become immutable versions under one dataset/platform/object-type scope.
- The Admin Token Data Center is an operator evidence surface: show the true canonical record, current revision payload, extensions and lineage without redaction. Public stored-search responses remain explicitly allowlisted and must never expose raw/source-private fields.
- Keep `/api/v1/data/search` backward-compatible with the Night-All adapter. Hub-owned canonical search is a separate endpoint; clients select logical dataset/platform/object filters, never a PostgreSQL database, Elasticsearch cluster, index, DSL or script.
