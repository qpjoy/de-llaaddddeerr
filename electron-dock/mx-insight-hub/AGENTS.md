# Prototype Instructions

- 2026-09-23 Douyin search session: three authorized TikHub Video Search V1 calls reproduced second-page 400 without search_id and 200 when the first response's data.log_pb.impr_id was supplied. Night-All now maps response-root log_pb.impr_id/extra.logid only for this verified Douyin endpoint, after explicit search_id/searchId. Do not broaden that alias to other endpoints, derive session IDs from request/item logs, rewrite old cursor state or automatically reacquire failed pages. Hub downstream cursor contracts remain unchanged; see docs/operations/douyin-search-id-2026-09-23.md.

- 2026-09-23 Night-All search timeout: default `NIGHT_ALL_TIMEOUT_MS` to 60000 across runtime config, standalone adapter and deployment templates. One deadline covers headers plus the complete response body; timeout remains an unknown outcome with no automatic paid retry. Existing deployments must update explicit overrides. Preserve other provider limits, reservation/settlement safeguards and MX-H2I login/networking.

- 2026-09-23 Xiaohongshu discovery products: group 笔记画卷 / 热门笔记 / 创作灵感 under 数据产品 → 小红书 without changing the existing note URL. New products default to API debugging with a second readable result tab, sharing identity and exact-request state; no mount/tab/renewal acquisition. `social.posts.hot_search` routes only to JustOne and `social.inspiration.list` only to TikHub, independently granted, priced, disabled initially (106) and visible in internal provider evidence. Use opaque product/Key/query-bound cursors; unknown native business rows remain complete delivery snapshots, not guessed canonical notes/ranks/metrics. Native response fields lack live schema attestation; disclose that boundary. Never invent procurement/customer prices or expand existing scopes; preserve MX-H2I login/networking. See docs/product/xiaohongshu-discovery.md.

- 2026-09-23 Facebook title and search compatibility: Facebook content follows Twitter's no-title policy in the historical Night-All path: standard raw title is empty, data-search/canonical title is null; preserve profile names, full body, raw evidence and immutable replay snapshots. Keep platform-specific hooks and no automatic historical rewrites. Hub composite continuations must retain stable query filters while replacing prior continuation state. Night-All raw search shares the standard search manifest's defaults (Facebook RapidAPI, wechat_mp JustOne); native Douyin cursor must coexist with search_id/backtrace, explicit hasMore=false ends traversal. Do not change grants, pricing, readiness gates, MX-H2I login or networking. See docs/operations/night-all-search-compatibility-2026-09-23.md.

- 2026-09-23 multi-currency procurement correction: operation price books under one provider may use different reviewed currencies. Admission and budget diagnostics must scope monthly call/reservation exposure by provider plus original currency; another request's valid USD ledger must not block CNY detail/comments as incomplete evidence. Retain same-request mixed-currency rejection, malformed-currency checks, same-currency budget/subsidy limits, funded customer-hold semantics and the existing transactional provider lock. Do not convert currencies or rewrite prices, holds, history, grants or MX-H2I state.

- 2026-09-23 structured Xiaohongshu topics: user explicitly rejects deriving tags from body text. Keep the entire body, including its existing topic markers; never remove them to avoid duplicate topic display. Render only API-provided tags as accessible links to the official topic-name search; empty arrays do not erase previously acquired list tags in the view. This supersedes the single-operation restriction only for missing structured tags: after successful nonempty analytics with tagsAvailable !== true, supplement once per note/Key page session through the existing authorized, runtime-ready body/tags API using cache_first. Keep separate billing evidence; no new aggregator or hidden server fan-out. Supplied empty tags, no-data, analytics failures, prior body results/failures and reopening must not cause another supplemental dispatch. Explicit body retry retains its original idempotency key. Never infer permissions, invent topics or change MX-H2I login/networking.

- 2026-09-23 Xiaohongshu detail rendering: upgrade HTTP image URLs only for known image CDN hosts (ci.xiaohongshu.com / *.xhscdn.com), preserving signed paths/queries. Do not reacquire to repair images. Detail metrics take priority; a wholly zero/missing set (including mixed zero/null) falls back to already acquired list metrics, and individually missing detail fields also retain prior values. Show each metric's detail/list origin and disclose whole-set fallback. Preserve individual zeroes in otherwise populated detail and never fabricate views absent from both sources. This is presentation-only; do not rewrite API evidence or add calls. Comment tabs have no unverified total and must not inherit page-level sticky offsets. Preserve existing billing/Key configuration and MX-H2I login/networking.

- 2026-09-23 Xiaohongshu comment discoverability: keep a visible note/comments switch above long detail content. The first explicit comment click requests one authorized, ready comment page; tab return, reopening, failures and credential renewal reuse session state and never auto-retry. Show comment-specific grant/runtime blockers in that view, keep pagination explicit and failed retry identity intact, and distinguish body/tags from analytics in the loaded status. Preserve MX-H2I login/networking and all existing Key/pricing configuration.

- 2026-09-23 tenant default pricing: migration 105 adds a tenant-wide final per-request price/currency, initially zero. Explicit assigned-plan entries (including zero) take precedence and retain their existing multiplier semantics. Default prices do not inherit plan multipliers; no priced plan is required for fallback. Preserve omitted fields from older billing-profile clients, use revision checks, freeze request-time evidence, and never rewrite existing plans, scopes, keys, balances or history. Only existing business usage meters are charged; aggregate live parents, login/admin/usage reads stay exempt. Preview and tenant price displays must use the same precedence. Deploy 105 before the binary; preserve MX-H2I login/networking.


- 2026-09-23 commercial governance: preserve all registered identities, Key secrets/status/scopes, grants, assigned immutable plans/prices, balances and historical ledger. Migration 104 adds only generated capability inventory and procurement-template metadata; it does not turn on billing, grant access or replace customer contracts. Keep reviewed product bundles shared across access shortcuts, sales templates and source mappings. Existing procurement prices start as protected exceptions; a reviewed preview/revision token and explicit apply are required for base-price inheritance. Product onboarding reuses published plans and existing grant/Key APIs. Consumption is paginated by charge with complete journal events, never reconstructed from a truncated ledger page. Aggregate price preview is read-only and never reserves funds or calls upstream; new live parents use an internal non-billable marker, while children and historical stored queries retain their existing rates. Deploy migration before the new server; preserve MX-H2I login/networking.

- 2026-09-23 Xiaohongshu click-to-open follow-up supersedes the earlier separate detail-button requirement: an explicit list/Mobile note click requests one authorized, ready detail operation (analytics first, otherwise existing body/tags); do not fan out or fall back after a dispatched failure. Reopen and temporary credential renewal retain the same note/Key session result and failed request identity; only explicit refresh starts a new successful query. Keep content first, fold diagnostics/evidence, and load comments explicitly. List history advances via pagination only; Mobile history needs a fresh user scroll gesture, never a layout/loader/ruler event. Grants do not override runtime controls or missing procurement evidence. Preserve MX-H2I login/networking.

- 2026-09-23 Xiaohongshu debugger follow-up: platform administrators can inspect all eight fixed API contracts even when the selected Key lacks a new scope; show Key authorization and runtime blockers separately and keep sending disabled. Tenant discovery remains current-Key-scoped. Provide provider-neutral external cURL examples and direct operation-control links; never infer grants or enable paid operations from catalogue visibility. Runtime disabled is resolved by the existing reviewed-price/explicit-activation flow, not by creating another Key or changing MX-H2I login.

- 2026-09-23 Xiaohongshu research: the note canvas adds explicit detail/read/exposure and comment-page requests through provider-neutral Hub contracts. Keep the existing full-body/tags operation: PGY does not guarantee structured tags or comments. Recommend at least five seconds between new analytics requests in UI/docs only; no new delay, buffer, automatic retry or fan-out. New operations `social.posts.analytics` and `social.comments.list` require independent consumer + Key grants and reviewed operation prices; migration 103 starts them disabled and never changes existing grants, plans or MX-H2I identity/networking. Xiaohongshu template v2 adds them at the existing CNY 10 minor units/request; retain v1 as an immutable four-operation template. Hide provider transport metadata on public compatibility deliveries while preserving full restricted archives. Comment cursors bind consumer/Key/note/sort and stop at 15 pages; missing counts are null, never fabricated zero.

- 2026-09-23 aggregate continuation/UI revision: use the shared Neon Void compact search bar and searchable multi-selects; default to all authorized live platforms, with historical data as a secondary mode. Explicit load-more continues the same round and appends deduplicated results; explicit refresh removes cursor and starts a new round. Never acquire on scroll/mount or restart exhausted/unknown sources during continuation. Bind opaque cursors to query, Key and committed child evidence; preserve stable root/page child idempotency even across parent-key retries.

- 2026-09-23 aggregate search: Data Browser has an explicit live-first aggregate tab. GET /api/v1/data/aggregate/sources discovers grant-scoped logical platforms; POST /api/v1/data/aggregate/search defaults to refresh (one page per continuing eligible source, opaque aggregate cursor, no stored fallback) or explicit stored canonical search. Never weaken existing Key scopes/quotas, change old search contracts, or affect MX-H2I authentication/networking. Parent request replay must not redispatch paid children; transient demo-credential renewal/tab navigation must retain the same identity's idempotency key. Tags/date are stored-only until a live contract can enforce them. See docs/architecture/aggregate-search-and-source-routing.md for current bounds.
- 2026-09-22 source inventory and aggregate-search direction: keep existing APIs and MX-H2I login/networking intact. Source catalog must distinguish authored coverage, implemented contracts, registered cleaning inputs and observed health; match reviewed paths by stable catalog IDs, never connector hints. One platform may have multiple sources by operation/request shape; default-route descriptions are read-only until an audited routing policy is implemented. Telegram monitor/SQLite are parallel stored inputs; Night-All-A cleaning and Night-All search remain separate. Future aggregate callers use Hub logical keywords/platforms/entry filters, never provider coordinates; documentation/product grouping never grants data access. See `docs/architecture/aggregate-search-and-source-routing.md`.

- 2026-09-21 Twitter title compatibility: Night-All Twitter content has no source title. Keep standard raw content title empty and Hub canonical title null; do not fill it from a platform label, body excerpt or legacy display name. Preserve account/profile names, all other platforms, raw evidence and immutable delivery/replay snapshots. Apply this to the historical Night-All content path only; no automatic historical rewrites, paid reacquisition or MX-H2I login/networking changes. See `docs/operations/twitter-title-compatibility.md`.

- 2026-09-17 page de-duplication and short pages: a page's declared count must describe the rows the envelope carries, and a row is a duplicate only when it repeats an identifier or is byte-identical content — an absent identifier is not an identity. Report how many rows de-duplication removed (`meta.duplicateCount`, alongside `resultCount`) so `returnedCount + duplicateCount` distinguishes low upstream yield from a de-duplicated page; the Hub-direct projection maps one-to-one and reports 0 rather than omitting the field. Requesting `count: N` stays a cap, never a promise to fill: both contracts document `returnedCount === 0` as the only termination signal, and server-side fill was deliberately removed from Night-All in August 2026 in favour of exposing full continuation controls. If filling is ever reintroduced it belongs in the Hub, not Night-All, because the route that matters is Hub-direct, every extra page is a paid provider call that only the Hub can cost-admit against quota and budget, and an internal fill keeps one public request bound to one Idempotency-Key exactly as detail enrichment already does. Preserve MX-H2I login/networking.

- 2026-09-17 request evidence, egress probing and count reconciliation: a paid acquisition's parameters are written by the reservation INSERT itself, so they precede any upstream work and an idempotent replay cannot rewrite them; cover every metered dispatch, keep `nlp.tokenize` exempt because its submitted text is the sensitive payload, redact credential-shaped field names and record an oversized body as a measured omission rather than a truncation. Historical runs without a stored body are reconciled by fingerprint verification, never by guessing: a read-only Admin endpoint recomputes the canonical fingerprint for a candidate body, proves or denies equality, dispatches nothing and discloses nothing on a mismatch. Connectivity-probe policy (timeout, attempts, selection-cache TTL) belongs to the route that owns the latency, so it is maintained on the Proxy Sequence in System Proxy and only overridden per service on that service's own binding; NULL is inherit, 0 is a decision, and a binary ahead of the migration falls back to application defaults instead of failing egress. A probe proves reachability, not upstream health: any status from the target origin selects the route and only the proxy's own 407/502/503/504 rejects it, every failed dispatch records its probe verdicts without proxy credentials, and selection caching stays off by default because skipping the probe downgrades a clean unbilled rejection to an unknown-billing outcome. A Night-All-owned envelope keeps its rows and its declared counts byte-for-byte; a contradiction between them is reported as a COUNT_DECLARATION_MISMATCH warning plus a read-time audit recomputed from the archive, and that warning must never reclassify the delivery, shorten its fallback window or change billing units. Preserve MX-H2I login/networking.

- 2026-09-17 acquisition comparison: keep historical delivery visible while explicitly sending a new request with original parameters and original Key identity. New comparisons have independent idempotency keys/request IDs; ambiguous retries retain the same key and body. Historical missing request bodies require clearly labeled manual input. GET replay and tab changes remain read-only; preserve MX-H2I login/networking.

- 2026-09-17 topic insights: distinguish research topic/keywords from source-category platforms. Keep keyword filtering, dynamic category selection and paginated report search aligned across the Hub workbench and public API/docs. Public report lists remain consumer-scoped; reads never create reports or dispatch upstream calls. Preserve MX-H2I login and networking.

- 2026-09-17 enterprise product workbench: expose 企业数据 under 数据产品 with API debugging first, using the existing current-Hub-Key identity and normal public billing/grants. Derive forms from the same OpenAPI contract as downstream documentation; search and paginate the catalogue, block unpublished-price interfaces, preserve exact-request idempotency across retries/tab changes, and never send automatically. Keep full documentation in 接口文档 with self-contained downstream examples, pagination/status/replay guidance. Use synthetic responses for development; preserve MX-H2I login/networking.

- 2026-09-17 Qixin pricing revision supersedes the initial disabled/manual-price bootstrap: paginate the official public catalogue and pin all displayed prices with evidence/date. The current snapshot contains 274 APIs, 260 publicly priced (including five zero-price results) and 14 negotiated-price APIs. Deployment enables only untouched publicly priced operations; preserve explicit operator pauses/custom policies. Negotiated/unpublished prices must fail closed in policy and dispatch even if an administrator supplies a price. Never expand consumer/Key grants on deployment. Make 启信宝 discoverable under 开放能力, provide explicit consumer + original-Key scope actions, and append the official-price template with a product-only markup/discount while preserving other rates and the tenant multiplier. Use search plus bounded numbered pagination for operation controls and large price tables; no load-more. Preserve MX-H2I login/networking.

- 2026-09-17 enterprise integration: add 启信慧眼 as an optional external platform using the reviewed `/tmp/company` catalogue and official Qixin Auth 2.0 contract. Manage App Key + Secret Key atomically through Admin-only encrypted credentials and second-token reveal; runtime signatures are server-generated. Forward only pinned catalogue operations, retain complete business responses, caller snapshots and Canonical response observations, and reuse explicit grants, per-operation pricing/rollout and paid-dispatch protections. Put the searchable 272-interface catalogue, parameters, examples and OpenAPI in 接口文档; keep the platform page focused on credentials, controls and evidence. No implicit paid queries, report polling, new grants or MX-H2I login/networking changes.

- 2026-09-17 Data Center layout: default to 检索与筛选 and place 搜索索引, 向量化 and 采集查询复现 in separate tabs. Keep datasets collapsed by default with an explicit expand/collapse control; selection filters records without losing search inputs. Tab switches preserve page-session inputs/results and never start indexing, vectorization or upstream acquisition. Preserve MX-H2I identity/networking.

- 2026-09-18 unreadable balances and recovery (migration `100`): a failed probe opens a `supplier_balance_unreadable` incident on the **second consecutive** failure (one blip is noise; the previous attempt's error code on the monitor row provides this, so never add a counter column), and any successful read closes it by provider, not by credential scope. Never render an unknown balance as a number. Recovery sends one message, guarded twice: only for an incident that was actually delivered (`notified_at`), and only for a monitor-recorded recovery (`recovered_at`), so a manual closure stays silent. A failed probe still cannot prove balance recovery. When a provider's balance and probe recoveries are both pending in one pass, send only the balance message and mark the probe recovery `notify_merged` -- marking it delivered is what stops it resurfacing later, so never merge by simply skipping the row.
- 2026-09-18 supplier balance Feishu delivery: Hub is now the only sender, reversing the earlier "do not send a second copy from Hub" rule. `/tmp/fee_monitor` is superseded and must not run alongside Hub. Port its rules exactly: one hour between reminders per incident, only a `code: 0` bot reply starts the cooldown, recovery sends nothing, and a new incident after recovery alerts immediately. Escalation to critical bypasses the window. Keep providers isolated -- one platform's missing/invalid/unreachable hook must never block the other or consume its window, and must record `notify_failed` rather than go silent. Bot hooks are operator policy on the balance monitor row (migration `099`, seeded with the groups the standalone script used), never deployment environment: an edit in the console takes effect on the next pass with no restart. `feishuWebhook` is optional on save -- omitted leaves it alone, empty clears it. Hooks are credentials: Admin listener only, never logged, never returned by an API, never written to the settings audit trail, and never echoed back to the console (DTO carries `configured` plus a short tail only). Delivery is never a readiness dependency and never touches MX-H2I login/networking or customer dispatch/billing.
- 2026-09-18 monitoring schedule follow-up: supplier balance checks run every hour on the hour, Asia/Shanghai (migration `097`, superseding the 2026-09-17 10:00/22:00 slots). Use fixed wall-clock slots, not a rolling interval. Saving/enabling, failures, credential rotation and restarts must not add extra queries; consume each slot before network I/O and skip missed slots after the five-minute dispatch grace window. Read-only account queries must not be advertised as free. Browser refresh only reads cached Hub observations.

- 2026-09-17 supplier fee monitoring: reuse the reviewed `/tmp/fee_monitor` balance contracts for TikHub and JustOne in an Admin-only, low-frequency Hub monitor. Show native-currency balances, observation time and explicit stale/error states in external-platform cards/details, with configurable warning/critical thresholds and durable `supplier.cost` notifications. JustOne defaults to CNY 5/3 since migration `097` (was 30/20), matching the standalone script's single 5 CNY line; TikHub's USD balance is never silently relabeled or converted to CNY (initial USD 5/3 defaults are editable). Reuse Hub credentials and TikHub egress. Page reads never trigger supplier probes. Never change MX-H2I login/networking or customer dispatch/billing on a balance alert.

- 2026-09-17 indexing observation: Data Center full-search rebuild and vector initialization must show measured ETA and stage timing/bottleneck clues. Clearly separate phase-only estimates, locked historical scope and changing daily queues; unknown, paused or stalled work must not show a fabricated deadline. Observation and deployment must never start online rebuilds or change operator budgets/concurrency.

- 2026-09-17 saved-record categories: the cleaning plan belongs to Night-All-A, not historical Night-All. Display “Night-All-A 数据清洗任务”; preserve existing internal source keys, dataset/platform IDs, mappings and checkpoints for compatibility. The original 13 source_type leaves are seed inputs, not a permanent taxonomy. Discover new categories from the read-only source catalog, scan daily (persist the last scan across restarts), automatically approve and activate newly discovered compatible leaves after source/schema/index checks under the existing reviewed writer guarantees, then use the normal ingestion/outbox indexing path. Persist activation intent; manual pause must cancel it so rediscovery cannot override operators. Show blocked/default-partition categories with truthful completeness limits. Expose provider-neutral category metadata and current-Key authorization via GET /api/v1/data/platforms (category-directory alias retained); discovery must never grant public data access or build source indexes or alter MX-H2I login/networking.

- 2026-09-16 notification decision: supplier balance/limit alerts belong in a Hub Admin notification center with explicit categories, durable request/call evidence and handling history. Keep acknowledgement and manual closure separate from proven upstream recovery; shared Admin Token actions must not invent a named actor. Supplier notifications stay Admin-only and independent of MX-H2I login, networking, public dispatch and billing. Do not display numeric balances or low-balance forecasts without a verified provider balance API.

- 2026-09-16 follow-up: external platforms share an integration-slot presentation (ownership, execution modes, delivery, mapping and evidence) while preserving each provider's authorization/billing/runtime semantics. Night-All-A defaults to bounded cases, explicit collection triggers, task/run observation and logs; documentation is secondary. Supplier manifest/adapter/verification packages are evidence to review, never automatic execution authority. Distinguish offline validation, live collection, data completeness and Hub ingestion; treat process collectors and remote HTTP platforms as different adapters of the same management contract.

- 2026-09-16: Night-All-A is a separate Admin external data platform, not a replacement for historical Night-All. Hub 100.127.0.6 reaches 100.127.0.1:8100 over existing OpenVPN; Nginx is optional. Register the complete upstream API catalog while forwarding only explicit reviewed business operations. Collection triggers and database cleaning plans are separate lifecycles. Unknown async submissions must not be automatically retried. Keep all changes independent of MX-H2I login/networking.

- Key “额度与用量” dialogs need a wide responsive layout: wrap metric cards by available dialog width, retain complete plan names/labels, and keep quota tables horizontally scrollable on small screens.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## MX Insight Hub product context

- Treat `/Users/qpjoy/workspace/qpjoy/de/de-llaaddddeerr/electron-dock/mx-launcher/demos/ui-design-neon-void` and `mx-launcher/ui-design` as the visual source of truth.
- Hub defaults new browser profiles to the cool light Neon Void theme and keeps the existing dark theme as a persisted user-selectable mode. Theme changes are presentation-only and must not change authentication, routing, or API behavior.
- Use the shared searchable `DropdownField` for every popup value selector in the Hub. Do not use native `<select>` for Hub product controls because its expanded menu cannot be themed consistently; region-picker dialogs and navigation accordions remain their own interaction patterns.
- For the governed source catalog, the real Hub `DashboardPage` command-center layout is the large-screen visual baseline. The existing external-source pipeline page is an execution-semantic reference only, not the dashboard reference.
- Keep `/source-catalog` as the governed business directory. Present source execution under the first-level “数据清洗中心”, with “数据库配置” and “清洗任务计划” as separate second-level surfaces; do not merge catalog status/storage with runtime source status/storage.
- Present live paid upstream API integrations under the same first-level “数据清洗中心” in the separate second-level surface “外部数据平台”. This surface is a provider command center, not a scheduled cleaning-plan list: it must distinguish Hub requests from actual upstream dispatches, successful upstream envelopes from usable canonical results, and idempotent replay, fresh cache, stored fallback, rejection and unknown outcome. A provider connector is optional and must never become a Launcher identity dependency, a Hub readiness dependency, or a reason for existing stored reads and admin/login paths to fail.
- Keep public data contracts provider-neutral even when one provider currently supplies them. The authorization platform and canonical dataset describe the Hub business product; the physical provider belongs in versioned adapter evidence, immutable response archives and canonical lineage. A caller-supplied `Idempotency-Key` identifies one exact request (including its page/cursor), so changing pages requires a new key; generated keys may provide bounded exact-request deduplication for clients that omit one. Never blindly retry an ambiguous paid dispatch, and preserve the distinction between an upstream `code=0` billed call and a response the Hub could successfully normalize.
- Database configuration profiles are optional transport/credential reuse only. A cleaning source/plan still owns its table locator, cursor, mapping, schedule and evidence; complete inline configuration remains valid, and fixed SQLite/custom-HTTP adapters are not forced into a database profile.
- Treat mobile multi-commerce collection as authorization platform `mobile_commerce`, with 快手/抖音/淘宝/闲鱼 retained as stable source-catalog/classification facets. Catalog related-data and public catalog-item queries must use the reviewed catalog-entry ID, not compare marketplace names to the authorization platform. Its first public capability is stored-only; rows follow canonical outbox → Elasticsearch projection/search. Any future remote refresh is a separately granted asynchronous command executed by an external mobile collector, while Hub owns only trigger/status, cleaning, indexing and data APIs; it is never an implicit public-read side effect.
- Present the Hub-owned virtual supermarket as one data product with three truthful modes: “逛超市” for guided shelf browsing, “超市全景” for a spatial renderer and “目录模式” for dense accessible management/browsing. The panorama is presentation only: public contracts expose stable department/aisle/shelf/position semantics, never WebGL coordinates, camera, lighting, meshes or materials. `virtual_supermarket` is an explicit Public API platform grant separate from `mobile_commerce` and `source_catalog`; its public reads are on-shelf-only safe projections. Off-shelf/archive changes the storefront publication record without deleting the referenced canonical capture. Preserve `storefrontRevision` in metadata/pages and bind opaque cursors to the complete normalized filters, sort, page size and revision. Mobile-commerce captures remain the immutable source observations; Agent Studio may propose category, specification or identity mappings with evidence, but cannot publish, merge, delete or move products automatically.
- Present the JustOne-backed search experience as the Hub-owned data product “电商数据” (default list view, with optional “百宝箱” view). Public docs and callers keep the provider-neutral `POST /api/v1/data/ecommerce/products/search` contract; Admin-only UI may disclose JustOne as the current adapter, lineage and capability atlas. The product defaults to Hub Open API with refresh delivery (2026-09-12 user decision); demo remains an explicit option, keeps any Hub consumer key in component memory only, dispatches on explicit search/next-page actions or user scrolling a confirmed continuation after key verification, and reports `live`, `fresh_cache`, `stored_fallback` and `idempotent_replay` without guessing cost from latency. In the source catalog, mark only 淘宝、天猫、京东、小红书店铺、闲鱼 as `JustOne · 商品搜索已接`; leave 抖音电商、快手小店 as `JustOne · 接入线索` until a runtime contract is verified.
- The fixed mobile-commerce v1 dataset worker reads one active PostgreSQL source-catalog snapshot per batch and fails closed on exact-column drift, malformed/non-finite checkpoints or rows, actual NULL writer-required values, or loss of the unique `id` identity index. Writer v3 admits `timestamp`/`timestamptz` plus a narrow mobile-only `text`/`varchar` watermark: textual values must be exactly 19 characters in `YYYY-MM-DD HH:mm:ss`, are ordered with PostgreSQL `COLLATE "C"`, and are interpreted as Asia/Shanghai before canonical UTC normalization; generic database sources remain unchanged. A timestamp `(collected_at,id)` index or textual `((collected_at COLLATE "C"),id)` index is a reported performance recommendation rather than a correctness gate when unique `id` already proves total order. Nullable DDL for writer-required fields is likewise an explicit warning governed by the current Writer attestation and a same-statement runtime NULL/format guard. `dsnEnv`, ID reuse, table replacement and public share/open-app tokens are outside this fixed contract.
- Treat `syncIntervalSeconds` as online scheduling policy for every cleaning plan: an interval-only save may run while active/running/draining, leaves current and queued work unchanged, and is reread on the next scheduler scan. Connection, locator, mapping and checkpoint-contract changes still require the existing pause/drain/probe gates.
- Keep `GET /api/v1/data/canonical/items/{id}/timeline` as the formal public bidirectional Telegram reading contract. The first call accepts independent `before`/`after` windows and returns ascending items plus `anchorIndex`; continuation calls return exactly one opaque HMAC cursor whose signed payload owns the older/newer direction and binds contract version, authorized dataset/chat stream, exclusive `(eventTime, canonicalId)` boundary, page size and authorization scope. Search cursors and timeline cursors are separate domains. Timeline currently supports only the two registered Telegram message datasets, requires the `telegram` grant, reads Hub stored data without invoking Telegram/upstream collectors, and uses live-keyset rather than frozen-snapshot semantics; it does not claim upstream completeness or a changes feed.
- Keep `mobile-commerce-data-processing` in Agent Studio as a compile-only, human-reviewed mapping-proposal template that captures source-specific identity, timezone, privacy, lineage and drift lessons. The fixed deterministic mapping remains the production path until an evaluated Agent artifact is explicitly approved; Studio must never imply that the template imports, publishes or remotely collects data.
- Data-cleaning navigation, shared database configuration and mobile-commerce ingestion are Hub-only changes. They must not alter MX Launcher/MX-H2I login, Domestic/Internal routing, WireGuard, DNS or existing user connectivity.
- Source-catalog metadata is authoritative in PostgreSQL. Elasticsearch may later receive a rebuildable outbox projection, but application requests must never dual-write PG and ES.
- Source-catalog status has four independent delivered axes: coverage (`unknown/not_covered/partial/covered`), delivery (`exploring/planned/doing/blocked/complete/paused/retired`), field review, and runtime health. A `complete` delivery may still have unknown or degraded runtime health.
- Use the supplied Sub2API dashboard screenshot only as the information-architecture reference for accounts, API keys, plans, quotas, usage, channels, and operational metrics. Do not copy its branding.
- The primary workflow is: create a consumer, issue a one-time API key, grant explicit platform capabilities and limits, call Hub-owned APIs, and inspect usage/latency/error evidence. Migrated operations dispatch through Hub-native provider connectors; only explicitly documented, not-yet-migrated compatibility shapes may still use the historical Night-All path.
- Keep API-key authorization and upstream routing orthogonal. A key's immutable snapshot explicitly selects a business data domain (for example `xiaohongshu`), an operation (for example `social.posts.resolve`), and any provider-compatible contract surface it may call; newly issued keys default to no paid/live or provider-compatible operation, and omission must never mean “inherit every current grant”. TikHub, JustOne, Night-All and future collectors are Admin-only routing choices, not ordinary tenant platform grants. A data product such as 小红书笔记画卷 is a named commercial bundle over these underlying scopes, not another redundant runtime gate.
- Provider operation rollout is database-governed and independently revisioned as disabled/shadow/canary/active/paused, with Admin actor/reason, reviewed contract release, credential revision and immutable upstream price-book evidence. Contract-verification environment flags are bootstrap defaults only. After an audited Admin operation write, database state is authoritative and hot-loaded for dispatch; pause/disable in the console replaces environment kill switches. Fixed-origin allowlists and technical transport limits remain deployment configuration. A missing provider credential, release or price disables only that provider operation and must not fail Hub deployment, readiness, stored reads, admin/login paths or MX-H2I connectivity.
- Every successful live or provider-compatible delivery must retain exact restricted upstream evidence and enqueue canonical ingest while preserving one downstream idempotency/billing identity. Distinguish current canonical search from historical replay: expose an owner-scoped acquisition/query-run view that binds the delivered response hash, ordered canonical revisions/observations and provider-call cost lineage by request ID; never claim that the latest ES/PG current-state query alone reproduces a past external result.
- Night-All remains an internal data source. Never expose its provider credentials, provider names, endpoint IDs, `businessId`, or `availabilityMode` to public clients.
- Paid upstream credentials and provider-management details stay out of tenant/Public and ordinary Admin UI. The Admin-token-only external-platform page may manage them, and may reveal a database-managed key only after an explicit second Admin Token check; no tenant session, Launcher session or Public API key may receive them.
- “Redaction” must never mean truncating, filtering or rewriting acquired business data. Preserve complete upstream business payloads (content, author/profile fields, tags, metrics, media, signed media URLs and pagination) in the restricted raw archive, alongside PG canonical/revision data and rebuildable ES projections. Provider-shaped, authenticated compatibility APIs preserve the upstream business envelope; Hub-owned product APIs may expose a stable documented projection. Only the Hub-to-provider request credential itself (API keys and Authorization/Cookie material) stays out of responses, ordinary UI and logs.
- Apply that provider-identity boundary to source-catalog presentation as well: raw Admin-token records may retain internal connector/lineage names, but tenant/Public projections and every visible UI path (including search, facets, CSV, detail and edit forms) must hide paid-provider identity without overwriting the authoritative record.
- Platform compatibility APIs may preserve familiar provider-style path names (for example `/api/v1/xiaohongshu/app/get_note_info`) when that reduces caller migration cost. These are still Hub-owned contracts: Hub tenant identity, Live API keys, grants, quotas, billing, idempotency, stable response projection, cache and canonical/archive lineage remain authoritative; keep the provider-neutral Hub endpoint in parallel and never expose the physical upstream or its credentials.
- Keep the three historical `/api/v1/search/raw|crawl|user-info` spellings as exact aliases of the Hub compatibility routes. Xiaohongshu variants that have a verified Hub-native TikHub contract must dispatch inside Hub rather than through Night-All; preserve the historical response envelope and one paid-operation fingerprint across aliases. Bound every explicit page and opaque traversal to at most 15 pages. For any still-historical Night-All traversal, expose only the Hub-encrypted `mxnc1` continuation bound to consumer, operation, platform, stable query scope and next page; clear continuation on page 15, reject pre-wrapper provider cursors/continuation params with `400 invalid_cursor`, and require restart from page 1 without a cursor plus a new `Idempotency-Key`. Use a new key for every next-page business request. This pagination-control rewrite is the only compatibility response governance mutation: never filter, redact or truncate business fields, long bodies, `raw_info`, `raw_data` or correlation values. The historical hop retains the complete parsed JSON payload and legacy raw strings; only Hub-native provider calls additionally promise exact upstream response text/bytes plus hash in restricted raw storage. The Xiaohongshu historical fallback of `/api/v1/data/search` uses the same `mxnc1`/15-page rule while existing `mxec2` direct Xiaohongshu cursors remain pinned to the Hub-native connector. Search previews at the known 60-character boundary are incomplete until a same-note detail result supplies strictly longer text; retain partial-completeness evidence when repair cannot finish.
- Record reviewed upstream procurement cost before every paid dispatch and keep provider/customer amounts in their original currencies. The monthly procurement/subsidy thresholds are hard admission limits only for requests without their own positive `enforced` per-request wallet hold; a correctly scoped, funded downstream charge must not be rejected merely for crossing those Hub financial thresholds, even when the two ledgers use different currencies. The exact current request still needs valid cost evidence, while provider rate/quota, concurrency, circuit breaker, contract, credential, idempotency and pagination protections remain authoritative. Downstream prices remain operator-entered immutable plan versions; never invent a customer rate, currency conversion or margin assumption in code or UI.
- For Xiaohongshu link input on that platform-shaped path, prefer JSON POST so temporary share parameters do not enter the request target, proxy logs or browser history. Keep GET `note_id`/`share_text` only as a compatibility surface, recommend `note_id` on GET, and keep delivery mode inside the exact idempotency fingerprint while sharing note identity for snapshots and paid-dispatch suppression.
- MX Launcher owns deployment and human-operator access. MX Insight Hub owns tenants, consumers, API keys, grants, plans, credits, usage, request idempotency, and billing evidence.
- Agent Provider catalogs must use page-level table/list CRUD with explicit create, edit, delete, test, and secret-reveal actions. Do not put whole-catalog Provider creation/editing back into a vertically stacked modal. Proxy endpoints and Proxy Sequences likewise keep visible CRUD on the page; Provider/Hub bindings are labeled as compatibility controls, while new business routing is selected on the LLM Sequence.
- Agent Market is a real, manageable catalog rather than a fixed demo gallery. Categories are operator-created records, Agents are persisted catalog records, and built-in examples must truthfully disclose whether they have an executable adapter and real run data. Seed a knowledge-Q&A catalog entry and place the existing advanced-search tutorial under a Demo category; never fabricate run counts, accuracy, latency, or health for display-only records. Custom Agents may be created and managed before an executor is attached, but the UI must label them non-runnable instead of simulating results.
- Keep the Agent Market stage inspector non-sticky on desktop, stretched with the workbench, and explicitly collapsible toward the right so the graph can reclaim space. Graph nodes, edges and loop decorations must remain clipped inside the middle workbench layer and never paint above the inspector. A zero-millisecond terminal Trace may be a real synchronous guardrail result; show its terminal status and reason instead of implying it did not run. Refresh continuity may use only bounded, redacted, Hub-admin-only browser-session history; it is not a server audit ledger. The longer-term Studio must separate authoring graphs, compiled executable artifacts and approved immutable deployments, with code-owned node/tool registries and no change to MX-H2I login or networking.
- Agent Center is the single first-level navigation branch for all model and Agent controls. LLM Provider, LLM Sequence, LLM Proxy, Agent Market, Agent Studio and the legacy runtime are its second-level items; Agent Studio must not appear as a separate first-level branch.
- Agent Studio opens on a portfolio that lists every Studio-owned Agent product and keeps Draft, compiled Artifact, Eval, Release, Deployment and Market visibility as separate truthful lifecycle facts. A concrete graph editor is a project Draft detail route, not the Studio landing page. P1 may enable governed Draft editing and server compilation only; unavailable Sandbox, Eval, Release, Deployment and Market publication steps must remain visibly disabled instead of being simulated.
- Studio LLM nodes reference an approved LLM Sequence. The effective Provider/model/Proxy route is resolved and shown as read-only evidence from that Sequence; Provider, Proxy and Sequence are execution configuration and must not be modeled as business graph nodes or request-level endpoint overrides.
- Source-catalog categories such as news, social media, enterprise, filing, entertainment, maternal/child, ecommerce, search, industry/commerce and legal are facets rather than mutually exclusive folders. A catalog entry is governance metadata, not a runnable connector: Studio and Data Center must not claim a source can analyze or import until a physical source, approved mapping and canonical dataset contract exist.
- Provider Catalog is an account directory, not a service default: Catalog order and the first record never imply a business default. Every business model call must resolve an explicit LLM Sequence, including the one-Provider case; Chat and Embedding may legitimately have no default. Direct Provider calls are limited to operator connectivity probes and rolling-migration compatibility.
- An Embedding Provider may explicitly inherit the connection, credential, timeout, and compatibility Proxy binding of one database-managed Chat Provider; it never copies the secret and never inherits the Chat model. The Embedding model and dimensions remain explicit. Known unsupported vendors/protocols are unavailable, known supported models use the maintained capability catalog, and unknown OpenAI-compatible gateways remain available only with a visible probe-required warning. Changing an inherited Chat connection invalidates the dependent Embedding revision and Sequence proof; no catalog entry is selected implicitly.
- LLM Proxy records are optional. Creating the first endpoint or Proxy Sequence never binds it. An LLM Sequence has an explicit three-state egress policy: inherit, Pod/Node system egress, or one ordered Proxy Sequence. Inherit resolves the compatibility Provider and Hub overrides first, then the deployment-observed Docker daemon proxy, and finally Pod/Node system egress when no daemon proxy applies. Show the observed daemon source and redacted effective values as read-only evidence; confirmation may unlock Hub-owned policy editing, but the UI must never edit or restart the host Docker/systemd service.
- Build Proxy Sequences by dragging only the dedicated endpoint/step handles, with insertion ordering and a drag-out removal target; cards, inputs, scrollbars and sliders must never be draggable. Keep arrow, add and remove buttons as keyboard/touch alternatives.
- Provider connectivity evidence is scoped to the exact egress route being verified. Creating or editing an unbound Proxy catalog entry must not invalidate a direct Provider test; changing an LLM Sequence's selected Proxy route, endpoint URL, endpoint state, order, or fallback policy must invalidate the Sequence proof. One-shot Provider × route tests require an explicit system/Proxy choice, never preselect the first record, never mutate bindings, and never write reusable probe evidence.
- Chat Provider and Sequence connectivity probes must leave enough output budget for reasoning-capable models to reach final `content`; reasoning-only output is never a successful Agent result and its chain-of-thought must not enter errors or logs.
- Records referenced by Agent control objects are protected by the server transaction, not only by disabled UI actions: a Provider must leave every LLM Sequence before deletion, a Proxy endpoint must leave every Proxy Sequence, and a Proxy Sequence must be unbound from every LLM Sequence plus the legacy Hub-global/Provider compatibility bindings.
- Reuse the shared Neon Void `qp-modal`/`qp-dialog` primitives and Hub `ConfirmDialog` for destructive browser actions. Do not add new `window.confirm` flows; dangerous dialogs initially focus Cancel and remain non-dismissible while busy.
- Server-file sources use a plain pasteable path input, never a directory/path dropdown. Treat the pasted absolute path as transient: runtime allowlists reduce it to `rootId + relativePath`, and catalog/log/response lineage must not persist the host path. “Archive by structure” means reuse an immutable format-rule version; never move or delete the source file.
- File registration may classify a value-free first/middle/last structural sample automatically. Known producers use a stable logical rule (for example `rule-twitter-canyie`) whose CSV/JSONL layouts become immutable versions under one dataset/platform/object-type scope.
- The Admin Token Data Center is an operator evidence surface: show the true canonical record, current revision payload, extensions and lineage without redaction. Public stored-search responses remain explicitly allowlisted and must never expose raw/source-private fields.
- Keep `/api/v1/data/search` backward-compatible with the Night-All adapter. Hub-owned canonical search is a separate endpoint; clients select logical dataset/platform/object filters, never a PostgreSQL database, Elasticsearch cluster, index, DSL or script.

- Ecommerce “全部平台” is stored-only and consumer-scoped; it must never fan out acquisition. Keep acquisition cursor/page separate from stored-history cursor and preserve each item’s originating request ID for media and evidence.

- 2026-09-12 ecommerce management revision: outer tabs are 数据列表 / 百宝箱. The default list automatically uses the existing Admin session to browse all Hub ecommerce observations (empty title filter, configurable 1–100 page size, default 10); do not require a consumer API Key for management reads. Pull-down in the phone panel explicitly acquires the selected single platform's next upstream page; upward scrolling only reads older Hub rows. All-platform view never dispatches acquisition. Admin edits/deletes are a revisioned management projection over immutable paid evidence, and manual additions are explicitly marked. Public consumer scope and original replay/media contracts remain unchanged.

- Ecommerce feed defaults to 10 displayed/history rows. Shared marketplace/title/price filters constrain history; only supported acquisition parameters go upstream. History remains time ordered. Pull-down prepends bounded batches and consumes already acquired overflow before another paid request; upward scrolling appends older history. Keep original image URLs, report unverified image fees as unknown, and offer strict cache-only reads until mx-static is explicitly integrated.

- Night-All is an Admin external data platform for legacy raw/crawl/user-info. Keep its zero service transfer price separate from customer plan prices and crawl work limits. Reuse existing raw/crawl/user-info billing meters and immutable plan versions; never automatically reprice tenants or double-reserve a legacy request. Night-All statistics count connector-backed logical requests, not preflight failures or replay HTTP traffic.

- Crawl admission uses consumer/platform maxCrawlWork independently of per-page limits and customer prices. Default 100, preserve lower legacy platform limits on migration, operator maximum 5000; never accept client budget overrides. Exact duplicate aliases in the same Night-All identity family count once; usernames and user IDs remain distinct. Reject before usage reservation/dispatch with calculation details.

- 2026-09-13 user decision: group platform-native documentation by JustOne and TikHub, with endpoint pages for the released Xiaohongshu list/detail scope. This explicitly supersedes the old vendor-free documentation navigation rule; provider labels and official reference links are documentation metadata only, never caller-selectable routing or credential disclosure. The note canvas offers persistent Hub browsing, explicit upstream acquisition and detail/body/tags. Keep Hub history cursors separate from upstream pagination and preserve MX-H2I login/networking.

- 2026-09-13 user decision: System Proxy is the shared Agent/TikHub proxy registry (legacy `/agent/proxies` URL remains). TikHub owns a revisioned, audited DB binding to inherited global policy, a specific sequence or direct egress. Migration 076 seeds the user-verified Internal host-network 7788 sequence only for TikHub, without changing LLM/global or JustOne routing. Fail over unauthenticated connectivity probes only; never retry an ambiguous paid call. Proxy changes must not change MX-H2I login/network/DNS.

- 2026-09-13 user decision: all data-product demos share a selectable identity, defaulting to the unique active Live Key named LCY-delta. Do not embed/recover its original secret. Admin-token-only demo credentials are short-lived signed references to the existing key; every public call rechecks key/consumer/tenant status and original entitlements. Keep quotas and billing on that original key. No new keys or grants are created; ambiguous defaults require selection. Browser state stays in memory and changing identities resets product views.

- 2026-09-13 note gallery: show acquired image URLs by default, with an off switch and explicit missing/disabled/failed states. Card details expose all acquired images, image-only reload, and an explicit live-only note refresh (possibly billed). Retain the same idempotency key and delivery mode after failure; never refresh a paid note automatically to repair an image.

- 2026-09-13 note detail revision: no backdrop/Escape dismissal; explicit close only, disabled during acquisition. Preserve detail result/error/idempotency state in the current identity-scoped page session. Use acquired CDN URLs for demo detail images, avoiding the server relay timeout; retain the public relay contract. Group actions with spacing. Consolidate the standalone link workbench into a collapsed entry above the list; detail includes delivery evidence.

- 2026-09-13 access revision: documentation is authenticated console content, including /docs aliases and OpenAPI JSON. Anonymous document navigation goes to the console login guard; authorized navigation uses the admin documentation endpoint and same-tab docs route. This supersedes prior publicly readable docs behavior. API Key management hides revoked keys by default with an audit toggle; revoked keys remain excluded from actionable demo identity choices. Keep consumer records and audit history independent of Key revocation. Launcher identities bind to Hub-owned members/tenants via the existing membership API; do not confuse a durable binding with an indefinite login session.

- 2026-09-13 tenant service access: new consumers have zero implicit capabilities, including NLP. Only platform administrators configure scopes/quotas; tenant owner/admin may issue subset keys. Migration 077 stores revisioned audited tenant defaults, synchronizes existing consumers atomically and serializes new consumer inheritance. Preserve separate old grants outside the previous/new tenant configuration; additions never expand existing key snapshots, removals constrain them immediately. Limits here are per consumer, not aggregate tenant budgets. Preserve Launcher/MX-H2I behavior.

- 2026-09-13 user decision supersedes one-time-only key display: newly issued API Keys have a separate authenticated encrypted vault (migration 078), with purpose-separated derivation from the existing pepper. Reveal requires fresh Launcher password verification, same logged-in member and current tenant key-management permission, no-store response and metadata-only audit. Never expose vault values in lists/logs. Historical hash-only keys cannot be recovered; require replacement issuance without automatic revocation. App V2 runtime diagnostics list blocked constituent operations rather than implying every endpoint is unavailable.

- 2026-09-13 tenant console revision: tenants with usage.read land on a personal usage/balance dashboard. Keep operational health and governance out of tenant navigation; retain own-service diagnostics through the dashboard. Source catalog remains a shared Admin governance directory, not a per-key copy. Demo identity is collapsible; tenant-entered Hub keys remain memory-only and use normal public grants, quotas, rollout and billing. Customer-specific plans bind to consumer versions; wallet/mode/multiplier are tenant-wide. Publishing never automatically reprices other consumers. The explicit publish/assign/enable flow preserves balance and reports partial completion; do not replay publication after later-step failures or retroactively bill old requests.

- 2026-09-13 follow-up supersedes tenant manual-key entry: tenant owner/admin product pages automatically select their unique usable Live Key, otherwise offer only their own keys. Use a short-lived signed key reference bound to the Hub member, never recover the original key secret. Recheck current member status and owner/admin membership on each public request using that reference; key/consumer/tenant status and original scope snapshots remain authoritative. Admin-token LCY-delta default and normal machine keys keep their existing semantics. Tenant billing and product copy must describe the user's services, prices and balances, without internal supplier/cost/privacy-policy explanations. Diagnose each requested operation independently across consumer grant, key snapshot and runtime readiness; disabled user-posts must not disable working search/detail.

- 2026-09-13 user revision: Xiaohongshu tenant/admin phone feeds share a vertical loaded-content ruler with clickable/draggable marks and page entry. Pages are recomputed from the current client list, not upstream cursors. Ruler navigation must never acquire data or auto-load history; user scroll and explicit acquisition remain separate. Debounce paid acquisition entrances and preserve in-flight locking and exact-request retry identity.
- 2026-09-13 documentation revision supersedes full provider labels: render J 平台 / T 平台, follow the console light/dark theme, and omit governance, generic tools, legacy compatibility and operator-evidence documentation from tenant HTML and downloadable schema, including direct authenticated routes. Preserve public API paths, authorization scopes and product/native contracts; documentation visibility never expands a Key's grants.

- Source catalog defaults to a platform/source connection dashboard with summary cards and a searchable category list, using Hub theme tokens. Keep the prior coverage/governance dashboard as the last tab with its overview deep link intact. Derive counts from active directory entries; never equate suggested methods, connector hints or template fields with verified integrations, live health or stored row totals.

- Tenant-facing product pages and documentation describe Hub API behavior, data freshness, authorization, usage and customer prices, without supplier/procurement narratives. Display native families as T Platform / J Platform. Filter tenant HTML routes and OpenAPI operations by the current tenant consumers' platform + capability grants, satisfying each combination within one consumer. Keep machine API paths/scopes/error codes unchanged. Xiaohongshu defaults to a normal paged list with optional Mobile view; both share identity, loaded results and request idempotency, and switching views never acquires data.

- Xiaohongshu now defaults to 接口调试 before the retained list/Mobile tabs. The debugger lists only current-key-granted fixed Hub endpoints, edits documented parameters, and sends only on explicit action. Preserve exact-request idempotency across retries and view changes; a fresh identical paid request requires the separately labeled new-request action. Never accept a custom destination URL or expose the selected credential in request previews.

- 2026-09-14: ipsearch is an optional upstream for the Hub-owned “IP 风险画像” product, with independent `ip_risk` / `ip.risk.query` grants. Tenant UI, docs, response and errors expose only Hub contracts. First release is explicitly metering-only, without customer charges or invented procurement prices. Keep exact responses and consumer-scoped result snapshots in PostgreSQL; do not implicitly publish private IP observations into shared canonical search. Multi-language collectors use versioned integration contracts and external runtimes when required. These additions must not change MX-H2I login/connectivity or make provider readiness an identity dependency.

- 2026-09-14 ipsearch follow-up: expose SDK single and batch query through fixed Hub IP-risk routes; batch preserves order/duplicates and meters each child. Reuse database provider credential storage and explicit second Admin Token reveal; saving a credential enables this metering-only product for already granted Live Keys, without expanding grants. Do not use real keys or send paid tests until the user resumes that testing; user will enter the key in the console.

- User revision: IP-risk single/batch clients need no Idempotency-Key; Hub generates a new internal identity when absent. Keep explicit-key compatibility for existing integrations, but hide it from the ordinary debugger and generated commands. Per-Key optional total/rate limits default to null and are independent of idempotency. Preserve existing configured policies and MX-H2I identity behavior.

- 2026-09-14 customer pricing update supersedes initial IP metering-only policy: `ip.risk.query` now uses the existing customer wallet/plan chain; built-in product template is CNY 5 minor units per successful IP (including valid no_data), while Xiaohongshu template remains 10. Unpriced operations and existing assignments remain unchanged until explicit publication/assignment. Definitive IP failures release holds; unknown outcomes retain them. Procurement prices remain unknown. Compose published plan versions/product templates at publication into one immutable price book and persist provenance in plan pricing JSON. Preserve consumer-level plan binding and tenant billing mode/multiplier; all keys under a consumer share its pricing. Do not infer authorization/docs visibility from pricing or alter MX-H2I identity/connectivity.

- 2026-09-16 data browser: keep the Admin-only 数据浏览中心 above the retained 数据中心. Browse stored canonical data without upstream acquisition or Agent calls. Account identities require platform plus stable ID, never display-name merging. Source-tag cooccurrence is a hotspot candidate, not a verified event or prediction. Agent-generated profiles/relations require versioned evidence and an evaluated runtime. Contract-driven forwarding with asynchronous Agent review is a plan only; preserve existing dispatch, billing, MX-H2I login and networking.
- Data browser lists must paginate narrow indexed IDs before reading bodies; never make first-page rendering depend on whole-corpus exact counts. Label page counts honestly. Search and bounded CSV/JSON exports share applied filters and Admin-only visibility; disclose partial exports. “Agent not integrated” describes browser capabilities, not the runtime status of all Hub Agents.
- 2026-09-16 user revision: 数据浏览中心 must provide corpus totals and total pages without making list rendering wait for a count. Keep exact statistics as an independently bounded, cached Admin-only request; expose its timestamp and retry state. Use the supplied Feigua images as the anatomy reference for account cards, profile/metric groups, content filters/tables and content detail, retaining Hub Neon Void themes. Card tags summarize up to 20 recent matching contents and disclose that scope; full profile tag distributions/metrics use the selected scope with field coverage. Source-derived statistics must never masquerade as Agent portraits, audience demographics or predicted events. Preserve MX-H2I login/networking and API forwarding.

- 2026-09-16 RAG decision: keep advanced retrieval Admin-only and separate from normal browser/dispatch. Fulltext supports bounded AND/OR/phrase/fuzzy queries and safe highlights; hybrid retrieval and optional cited answers must revalidate current canonical evidence. Vectorization uses its own durable worker queue and strict HanLP without tokenizer fallback. User explicitly starts full ES rebuild and whole-corpus vectorization from 数据中心; do not start online replay/rebuild automatically during deployment or agent work. Future LLM Wiki remains a versioned derived evidence layer, never canonical truth.
- 2026-09-16 initialization revision: explicit historical vectorization captures a fixed record/revision manifest and has its own lifetime token budget (0 means unlimited). Never raise or reset the daily incremental budget on its behalf. New records and later revisions use daily budget, cannot extend initialization or spend its quota, and superseded versions are shown separately from completed vectors. Keep all full runs user-triggered. Capacity diagnostics must be read-only and exclude credentials and source bodies.
