# 数据浏览中心：实现边界与后续交付

日期：2026-09-16。本文区分已经实现、待验证和规划；参考项目文档中的目标不代表 Hub 当前能力。

## 本次实现

新增 Admin Token 专属 `/data-browser`，导航位于原 `/data-center` 之前，原数据中心保留。
新增只读 `GET /internal/v1/admin/data-browser`，不改变公共 API、上游派发、账本、Launcher 身份或网络逻辑。

| 视图 | 当前可用 | 明确未实现 |
| --- | --- | --- |
| 账号大盘 | 全量 canonical 范围内按平台＋稳定账号 ID 聚合；名称/ID、平台筛选；记录和内容数、最近采集时间；分页 | 粉丝时间序列、跨平台身份合并、Agent 标签画像 |
| 账号详情 | 固定平台和账号身份的所有入库记录、来源标签分布、发布区间、同标签账号候选，保留资料记录，可打开逐条详情 | 独立画像快照、相似达人排序、周期关联任务 |
| 内容大盘 | 所有类型当前入库记录；标题/正文和平台筛选；来源标签；账号跳转 | 自动舆情预测、商品统一规格模型、全库指标趋势 |
| 内容详情 | 全部 canonical 当前字段、正文、已有指标、按需图片/视频/音频预览、媒体链接和扩展 JSON；标签与账号关联 | 尚未采集的评论/视频自动补采；推断类分析 |
| 热点线索 | 最近 7 天发布的同标签至少两条记录，按最近 24 小时数量排序；标签关联列表 | 已确认事件实体、事件合并、跨语义关联、增长率及未来预测 |

账号统计的单位是 canonical 当前记录，不是去重后的跨数据集全网作品；列表明确“入库内容 / 总记录”。稳定 ID 缺失的记录仍可在内容大盘查看，但不把作者名当成账号身份。只识别 `user/account/profile` 为资料对象；后续来源类型必须经过语义映射。商品、评论、帖子等都保留原 objectType，不强行称为视频。

标签来自 `stable_fields.tags` 的字符串元素；非数组、非字符串和过长标签不进入热点聚合。标签重复不重复计数，未来时间、缺失发布时间、软删除记录不参与热点。热点进入关联列表后展示全部历史同标签内容，页面明确两者时间范围不同。没有实体聚类之前称“热点线索”，不宣称同一事件。

## 运行约束

- PostgreSQL 是数据真相；本次不使用 ES、不创建索引版本、不重放 outbox、不调用上游和模型。
- 每个进程每个 store 最多同时接受 2 个浏览查询，超出返回独立 429；每次聚合在独立只读 repeatable-read 事务内完成，statement timeout 为 3 秒，超时返回独立 503，不伪装成零数据。列表只取 200 字正文预览；详情按 UUID 读取完整当前字段。
- 参数由 Zod 白名单验证，拒绝重复字段、DSL/索引名等未知字段；账号筛选必须带平台；SQL 参数化。每页 1–50，最多 500 页。
- 全量聚合可能扫描大表。3 秒超时只是失败边界，不是容量保证。上线前必须在脱敏生产规模副本执行 EXPLAIN (ANALYZE, BUFFERS)，检查 CPU、连接等待、p95 和并发影响。高基数数据应迁入独立增量统计表或只读副本；不要取消超时硬扛。
- 单次请求内总数与分页一致；不同页面属于实时读取，期间入库可能让 offset 分页移动。未来超大规模导出用绑定过滤和快照的 cursor，不能把目前列表当完整导出。
- 普通 UI 复用现有 Data Center 可见性投影，隐藏供应商和 raw 管理字段；权威记录不被重写。完整原始证据继续由原 Admin 数据中心接口管理。
- 内存开发模式返回明确“需要 PostgreSQL”错误；不自动生成样例或假统计。浏览器夹具只用于测试，不进入产品代码。

## 仍需交付的数据分析链

先完成 [Agent 与检索可行性](./agent-search-feasibility.md) 中的执行和评估闭环，再接入画像和事件分析。建议拆分：

1. `account-profile`: 已采集资料＋有上限的作品样本 → 标签候选＋证据 ID/revision。画像单独版本化，不改 canonical。
2. `account-relations`: 入库变更触发候选召回＋周期修复，关系含类型、方向、置信度、证据和有效期。稳定身份关联与“内容相似”分开；不得自动合并账号。
3. `content-analysis`: 摘要、主题、情感、质量和结构化实体；明示样本/评论覆盖率。来源文本不能成为工具执行指令。
4. `event-clustering`: 时间窗口＋关键词/向量召回＋去重与聚类 → 事件候选；同标签不是事件合并依据。
5. `signal-detection`: 以时间序列计算增量/加速度/异常分位数；缺少基线显示“证据不足”，禁止零分冒充无风险。提示“需关注”而不是未经评估的预测。

拟增表：`analysis.runs`、`analysis.results`、`analysis.relations`、`analysis.events`、`analysis.event_members`、`analysis.signal_snapshots`。共同保存 input revision/hash、artifact/sequence 版本、evidence IDs、confidence、generatedAt、expiresAt、status、错误分类。`proposed/accepted/rejected/stale` 与运行成功/失败是不同轴。查询时 revision 不匹配标 stale；删除与撤销通过 outbox 使分析失效。

画像幂等键为 `agent + artifactHash + subjectId + inputRevision + policyVersion`。周期任务只处理变更、过期或漏算对象，租约抢占、重试退避和预算均在 Worker；原入库事务只发分析意向，不等待 LLM。跨账号关系保存双方 revision。上线应先离线评估和 shadow，小比例灰度，保留一键停用 Worker 和回退旧结果。

## 验证和上线门槛

本地检查包含类型检查、构建、只读参数/事务/超时/权限测试，以及原身份、数据中心、上游网关、重索引和 Sites 回归。UI 使用隔离内存服务检查真实错误状态，再使用明确测试夹具检查账号→内容详情→热点关联和桌面/窄屏布局。

另外通过临时 PGlite 嵌入式 PostgreSQL 实际执行查询，验证跨平台同名账号、全样本标签计数、重复/畸形标签、删除/未来/缺失日期、长正文详情、空页和参数字面量。该依赖仅在临时目录，未加入项目。

尚未执行部署态 PG/ES 集成、生产压测、线上 MX-H2I 登录或真实付费接口调用。因此这是可评审的只读基础版本，不能声称完整 Agent 数据分析中心或已经验证线上容量。部署前先满足 PG 验证门槛；本次不执行生产部署。
