# 小红书阅读、曝光映射与上游拒绝排查

搜索和普通详情规范化保留笔记自身明确返回的 `view_count` / `read_count`、`impression_count`，兼容 `readNum` / `impNum`；同时保留 `interact_info` 中对应计数。缺失、非法或负数保持 null，真实零保持零。新搜索记录入库和兼容投影保留这些指标，不重写历史记录。

画卷继续使用现有规则：详情优先；详情指标整体为零或缺失时使用已取得的列表/正文详情指标，逐项缺失也保留此前数据，并标明来源。普通搜索/详情并不保证返回阅读、曝光，不能把点赞或话题总浏览量当作笔记阅读量。

已接入的 [TikHub 蒲公英笔记详情](https://docs.tikhub.io/479321007e0) 是明确提供 `readNum`（阅读）和 `impNum`（曝光）的接口，路径为 `POST /api/v1/xiaohongshu/pgy/get_note_detail`。上游可能返回无数据或零，不能保证每篇笔记都有这些指标。官方说明 HTTP 200（含无数据）计采购成本，HTTP 400 不计采购成本；不能因为指标空而自动重复请求。

本次截图请求 `2a093bc5-f63f-4ac7-a2e4-2e6162dbba48` 返回 `external_platform_rejected`。按当前网关分类，它属于上游请求拒绝；成功响应无法规范化另报 `external_platform_response_unusable`。因此仅凭截图不能判为字段兼容问题，也不能确定具体上游原因。需要用该请求 ID 查询受限调用证据中的 HTTP 状态、业务码与错误信息。不得向下游暴露供应商凭据或完整内部响应。

此补丁不更改权限、价格、采购预算和失败重试策略，不自动调用其他付费接口。部署前后端后新获取的数据使用新映射；已有不可变交付不会被改写。
