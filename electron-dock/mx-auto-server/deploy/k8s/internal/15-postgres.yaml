apiVersion: v1
kind: PersistentVolume
metadata:
  name: mx-auto-postgres-pv
  labels:
    app.kubernetes.io/part-of: mx-auto
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: mx-auto-postgres
  hostPath:
    path: /var/lib/mx-auto/postgres
    type: DirectoryOrCreate
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mx-auto-postgres-data
  namespace: mx-auto
  labels:
    app.kubernetes.io/part-of: mx-auto
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: mx-auto-postgres
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: mx-auto-postgres
  namespace: mx-auto
  labels:
    app.kubernetes.io/name: mx-auto-postgres
    app.kubernetes.io/part-of: mx-auto
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: mx-auto-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mx-auto-postgres
  namespace: mx-auto
  labels:
    app.kubernetes.io/name: mx-auto-postgres
    app.kubernetes.io/part-of: mx-auto
spec:
  serviceName: mx-auto-postgres
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mx-auto-postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mx-auto-postgres
        app.kubernetes.io/part-of: mx-auto
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: fix-permissions
          image: postgres:17-alpine
          command: ["sh", "-c", "chown -R 999:999 /var/lib/postgresql/data"]
          securityContext:
            runAsUser: 0
            runAsNonRoot: false
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
              add: ["CHOWN"]
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
              ephemeral-storage: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
              ephemeral-storage: 64Mi
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: tmp
              mountPath: /tmp
            - name: postgres-run
              mountPath: /var/run/postgresql
      containers:
        - name: postgres
          image: postgres:17-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: mx_auto
            - name: POSTGRES_USER
              value: mx_auto
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mx-auto-secrets
                  key: MX_AUTO_POSTGRES_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: tmp
              mountPath: /tmp
            - name: postgres-run
              mountPath: /var/run/postgresql
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "mx_auto", "-d", "mx_auto"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "mx_auto", "-d", "mx_auto"]
            initialDelaySeconds: 30
            periodSeconds: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
              ephemeral-storage: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi
              ephemeral-storage: 1Gi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: mx-auto-postgres-data
        - name: tmp
          emptyDir:
            sizeLimit: 256Mi
        - name: postgres-run
          emptyDir:
            sizeLimit: 64Mi
