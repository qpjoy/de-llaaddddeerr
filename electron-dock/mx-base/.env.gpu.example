# Shared allocation, read by both GPU service managers. Indices or full GPU UUIDs.
# Copy to .env.gpu on the GPU server; never commit host-specific settings.
MX_BASE_DISPLAY_GPU=3
MX_BASE_OCR_GPU=2
MX_BASE_EMBEDDING_GPU=1
