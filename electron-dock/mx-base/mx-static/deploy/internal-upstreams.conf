# Include inside nginx's http {} context, NOT inside a server {} block.
upstream mx_static_writer { server 127.0.0.1:18200; keepalive 32; }
upstream mx_static_reader { server 127.0.0.1:18201; keepalive 64; }
