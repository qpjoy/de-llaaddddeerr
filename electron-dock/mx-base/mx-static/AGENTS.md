# mx-static NAS production safety

The NAS tooling is a production data-management component. Read `nas_docs/SAFETY.md` and the selected project/task registration before changing migration, recovery, permission or cleanup behavior.

## Established production state

**Latest incident supersedes the earlier readiness/deletion instructions below.** On 2026-09-24 at 17:08+ the user confirmed a SECOND SSD fallback: all ten infra media containers created at 11:04:11–11:04:39 lack NFS/NoCopy child mounts and kernel media source is XFS `/dev/nvme0n1p1` at `/app/media`. Nine app images are now `sha256:f0b13dc7f35c2d317a48be2ff3b6ef97e62b8f007d32c0c8af422377d7b07446`; gateway remains `6769dc3…`. Current Compose labels list only the two original files under `/home/lcy/test/Delta/mx_data`. Earlier NFS success belongs to old container IDs. The supplied last 80 reclaim journal lines contain only CHECK jobs, no actual deletion completion; do not infer either manual deletion or its absence. `/data` has ~34 GiB free, 99% used. Recovery is blocked by wrong media mounts and installed/current snapshots differ. The disabled Docker-wide RequiresMountsFor drop-in does not add container NAS mounts.

Current Git selection clears the stale reclaim plan to null; preserve all historical reports/receipts/NAS markers. No SSD deletion, old copy/cutover, direct media recreate or old-image fallback now. The returned `nas infra repair inspect --json` receipt has snapshot_stable=true, no review items, all ten current Compose/launch checks passed, nine reviewed startup script hashes unchanged and no writable app-code edits. PostgreSQL/Redis retain the original healthy IDs. No completion or nonempty intent ledger was found under the current report; this does not exclude manual deletion. Based on this evidence, repair prepare/copy/switch now pin the exact f0b13dc7… app image, with all existing live/config/script/DB/union guards retained. Old 45f5… repair plans are rejected. Fresh prepare succeeded at `/var/lib/mx-static/nas-repair/infra-1dfa9196bd8c4e32823ca73276630c74`: 198,783 quick matches / 500.33 GiB; 864 SSD-only files / 0.62 GiB (no tmp); 2,359 NAS-only / 1.18 GiB; 1,681 NAS attributes retained / 0.68 GiB; 101 shared differences hash equal / 0.16 GiB (0.31 GiB read on both sides). Next is unlimited `nas infra repair copy` using this NEW report. No second-fallback copy or switch completion is confirmed; this is an online snapshot, not cleanup readiness.

User screenshots establish both maintenance entries are `scripts/deploy_public_ghcr.sh --local-build` in `/home/lcy/test/Delta/mx_data`: infra uses port 59201 without --instance; delta uses `--instance delta-59202`, port 59202 and --db-port 55432. The original script supplied only app Compose files and recursively chmodded /app/media. The user has now explicitly authorized changing this one po-infra release script ("可以改该部署脚本"); this supersedes the earlier mx-static-only restriction for this file, not for business code. The local script is patched; the reusable patch lives at `deploy/nas/integrations/po-infra-release.patch`. It calls installed `scripts/nas/release.py` before env/git/task changes and for all Compose operations; NAS mode excludes raw-media from recursive chmod and checks after startup. The guard uses existing runtime/registration and lock, the local Docker socket, current app Compose/env, and `part1.release.json` (native external NFS plus external existing data volumes). It blocks SSD consumers, missing/changed storage and bypass run flags, preserves app launch/env and database/queue volume mappings, and keeps unmigrated delta local. It does not automatically run app deployment, replace its business initialization, or intercept arbitrary raw Docker commands. Installation/application on the server and a new NAS switch remain UNCONFIRMED. Do not use a full app release to repair current SSD writes. See `nas_docs/operations/release-guard.md` and `2026-09-24-second-fallback.md`. Older entries below are chronological history, not current permission to delete.

- On 2026-09-22, `po_infra_media_data/data_hub_raw_media` (task `part1`, project `infra`, Compose `mx_data`) successfully switched to the NAS. NAS remains the intended authority; the historical receipt is NOT proof of current placement.
- On 2026-09-24, user diagnostics confirmed all ten infra consumers were recreated on Sep 23 without the NAS override and now use the SSD parent (kernel XFS). Nine application images and `.env.ghcr` changed; recovery correctly blocks configuration drift. Both NAS and SSD now have unique files. Do not clean SSD, repeat the old copy/cutover, reuse the old pinned-image override for the new version, or reset report hashes to bypass checks. See `nas_docs/operations/no-ssd-fallback.md` for evidence and recovery requirements.
- Earlier live comparison: SSD-only 5,219 files / 2,867,924,180 bytes; NAS-only 1,032 files / 358,919,683 bytes. All 79 shared size/mtime differences hash identically; 1,681 tmp files differ only in mode (SSD 0644, NAS 0600). Later additions, successful NAS repair and business acceptance are recorded below; cleanup remains unapproved and unexecuted. These earlier live results do not replace a final stopped-writer check.
- Subsequent server checks on commit `b713ae7` confirmed the ten SSD mounts. Current deployment review passed: all Compose hashes and launch fields checked matched, reviewed startup scripts unchanged, no writable app-code edits, Postgres/Redis same IDs and healthy. The nine app services use image `45f5a0e5cae63bd1bc6215bcdbbe6531ba149ccc86dc47e11fcb55d1a9f5e0bf`; gateway uses `6769dc3a703c719c1d2756bda113659be28ae16cf0da58dd5fd823d6b9a050ea`. New `infra repair prepare` pins these images and writes a separate private UNION review; the successful server receipt is recorded below. It does not copy, execute, adopt a baseline or authorize cleanup. Do not pass its report to legacy cutover/redeploy/reclaim.
- Repair preparation subsequently succeeded at `/var/lib/mx-static/nas-repair/infra-a0131341096e4ba9b9e3e61c3e2581dd`: SSD-only 5,289 files / 2.69 GiB, NAS-only 1,032 / 0.33 GiB, attributes-only 1,681 / 0.68 GiB, all 79 bounded shared hashes equal. `infra repair copy <report>` provides manifest-only, no-replacement NAS additions with local durable journals and SSD read-only protection. Full online completion and subsequent NAS restoration are confirmed below; cleanup is not. Copy never adopts a new recovery baseline; interrupted private staging/hardlinks must be reviewed, not deleted blindly.
- The latest verified SSD reclaim inventory contains 200,543 files / 538,031,658,699 logical bytes. The accepted check receipt below establishes business acceptance and readiness; it does not authorize or establish deletion.
- `delta_59202_media_data` (task `part2`, project `delta`) has no reviewed cutover or cleanup record yet. Do not reuse Part 1 identities or configuration.
- On 2026-09-22, recovery snapshot `bd7b343be731926be9c8` was installed and enabled successfully with the unified migrated policy: infra verified/included, delta waiting for migration, service active/exited and timer active/running + enabled. No real reboot, NAS late-start or outage drill has been confirmed. This receipt does not establish business acceptance or SSD cleanup.
- User preference: NAS policy, tools and project-specific operations live in mx-static; do not modify po-infra for this work. The user uses root to operate tools and distributes changes through Git. Runtime reports and credentials stay private on the server.
- User requests no bandwidth throttling for migration: `infra repair copy` is unlimited by default; use `delta copy --unlimited` for the separate large-volume precopy. Preserve integrity checks, no-overwrite repair behavior and durable journals.
- Latest requested endpoint/order: finish Part 1 through verified NAS operation, business acceptance, current recovery registration and a fresh verified SSD reclaim manifest, but retain SSD until the user separately chooses deletion; only then advance Part 2 through its own equivalent process. Do not start Part 2 merely because Part 1's online addition finishes. Cleanup authorization is deferred; a saved readiness check must be revalidated at actual deletion time.
- Server job `mx-nas-part1-repair-copy-6c31173829.service` failed before creating a copy attempt (`attempt_directory=null`), during the all-candidate source preflight. The refused path was `avatar/3faed67853eeca8cd4bb18a4a290ccd06c84faeb27ecdab5fedda28df0479ec6.jpg`. The generic error does not distinguish changed metadata from an unsafe type/link/device; diagnose against the saved manifest before retrying or relaxing checks. This attempt copied no NAS files.
- Follow-up metadata confirmed ONLY `ctime_ns` changed for that candidate (1790161009298843605 -> 1790187923642721263), regular file with nlink=1. This does not prove content identity or the cause. Repair copy now rehashes only ctime-only drift in SHA256-named SSD-only candidates: the digest must match the original filename, every other metadata field must match, and the read must be stable. A private per-attempt `source-revalidation.json` records old/new metadata and SHA256 before NAS staging; original manifests/reports remain unchanged. Temp/ordinary filenames or any other drift still fail. Changes during copy retain strict ctime checks.
- Retry `mx-nas-part1-repair-copy-4012630bf6.service` / attempt `copy-77d14666313d4db99e3120d8d7531c50` confirmed at least 5,100 new files / 779,659,473 bytes before failing at the source-open check for `video/16bfbfff6fd9c5e09c188fdf14f2ff2401ad51624b1aaf054a6430c4215f5b4b.mp4`. Its changed fields are not yet known; do not assume ctime-only or media corruption. Keep completed NAS files and both sides' evidence. Repair copy now applies the same bounded ctime-only/hash-filename proof once immediately before each file starts, fsyncing `source_ctime_revalidated_before_copy` into this attempt's copy.jsonl; the original manifest and preflight snapshots remain unchanged. Changes during hashing/copy/publication still fail without retry. Initial `source_ctime_revalidated` counts batch preflight only; per-file revalidations are in the journal. Existing NAS matches are verified and retained, not rewritten.
- Latest server receipt: `mx-nas-part1-repair-copy-0096322ad3.service` succeeded with `nas_repair_copy_complete`, attempt `copy-220e052707a64e0aa53f2208b54448e2` under the above repair report. `copied=181`, `already_present=5108`, `logical_bytes=2892300834`, `source_ctime_revalidated=83`; all 5,289 prepared candidates accounted for. It remains a live snapshot: no production restart, no source deletion, stopped-writer recheck required, `reclaim_ready=false`.
- `infra repair switch` succeeded on the server: unit `mx-nas-part1-repair-switch-194cd1338d.service`, new report `/var/lib/mx-static/nas-cutover/po_infra_media_data-33e5abb193d04e7595251a5e6a6046ae`, final review `repair-final-11cccd4afb1243a79a84ff98015babe9`. Phase `running_on_nas`, final sync passed, actual mounts/HTTP/application probes passed, all ten NAS IDs recorded; PostgreSQL/Redis remain original containers. It handed the NAS marker to the new report with old marker/execution preserved in `repair-lineage.json`; old report files remain unchanged. No legacy sync/quarantine/SSD restore occurred. This is a successful repair, not cleanup or business acceptance.
- Git Part 1 registration initially selected the new report and cleared the old `plan` to null; it now explicitly selects the accepted plan recorded below. `infra cleanup check` creates a fresh source/NAS manifest without deleting or stopping anything; it compares SSD to stopped-writer evidence, retains NAS-specific attributes, and bounds mtime-conflict hashes. `--business-accepted` records actual acceptance only; user initially answered **not all business checks completed**, then returned the successful accepted check recorded below. Readiness requires actual acceptance and live recovery checks to pass. Reports retain SSD; they are not auto-selected for deletion. The UNION-aware later deleter requires a ready, explicitly registered exact plan, current recovery, and strict source/NAS metadata revalidation. The subsequent request for the selected SSD cleanup command is recorded below; no completion receipt yet.
- Earlier server follow-up (resolved by the later independent registration): all ten kernel NAS mounts match; recovery snapshot `/usr/local/lib/mx-static-nas/f85c883612ed4eaf9e23` installed, server reports installed/current equality, timer active/enabled. However `recovery-enable-migrated` refused infra with `Deployment files changed since preparation.` Policy was not changed; the `&&` sequence stopped before its subsequent recovery check and cleanup check. Subsequent read-only diagnostics prove the server still selects old report `830225384207402a8ba23a2364d252d1`, while all three deployment files, both rendered configurations, live consumers and databases match the new successful report `33e5abb193d04e7595251a5e6a6046ae`; files were stable during the check. Correct the server's stale registration through the current Git declaration and reinstall/check recovery; do not infer new application drift, reset report hashes, repeat switching, or bypass the guard. Subsequent successful check receipts are recorded below.
- User's implementation scope: change only mx-static, no po-infra source changes. Ordinary NAS recovery does not read app directories/env/API or bind historical images/IDs. The latest authorization includes starting existing PostgreSQL/Redis before dependent media services and waiting for current health conditions; it still does not bind historical database identity. Explicit creation/recreation/build may depend on the current app Compose/build. New media-v1 recovery and a separate maintenance adapter implement this locally; independent registration and recovery installation are now confirmed below. The explicit recreate path has not been exercised on the server. `storage register` creates a private media identity record independent of historical app hashes; missing registration blocks without legacy fallback. Recovery never rebuilds, creates volumes or authorizes cleanup. External Docker/old deployment commands are NOT intercepted. See `nas_docs/operations/media-runtime.md`.
- Explicit `deployment recreate --maintenance [--build]` is only for registered infra media roles. It checks existing data volumes, snapshots current app config privately, optionally builds media images, verifies maintenance startup, stops/creates/starts only media consumers, and leaves databases/queues to the business platform. Original Web startup calls bootstrap_admin which updates existing admin passwords; preserve the established direct Gunicorn maintenance launch and worker recovery disable. Do not execute the app release script, migrate, bootstrap_admin, recursive chmod or create missing data volumes as storage recovery. Incomplete maintenance leaves a persistent marker blocking boot recovery of partial/mixed containers. Server has not run this new path; current NAS-running consumers need no rebuild merely to adopt media-v1.
- Part 2 cutover/reclaim remains unimplemented. No reboot/reinstall/outage drill or enforcement of external deployment entry points has been confirmed. See `nas_docs/operations/part1-repair-switch.md` and `nas_docs/operations/part1-union-reclaim.md`.
- User confirmed one project entry with dependency startup: `nas infra start` and boot recovery now start only stopped existing registered PostgreSQL/Redis/media containers, using current Compose dependency labels (reviewed fallback for old missing labels), health conditions and current NAS checks. Running services are left alone. Missing containers/volumes, unreviewed dependencies, cyclic/one-shot dependencies, or empty PostgreSQL data block recovery. The same Docker project remains under po-infra application ownership; raw recreation still must include NAS declarations. The server has since verified existing services and installed/enabled this recovery path; no actual stopped-service startup or reboot drill is confirmed.
- User's final deletion boundary: migration can delete only within its authorized, verified workflow; ordinary restart, deployment and fault recovery must never delete NAS files or delete/clear dependent databases/queues. No new migration/SSD deletion approval is implied. The independent archive worker no longer schedules or executes NAS purges; old purge records, NAS publication staging links and failures are retained. Existing conflicting NAS objects/metadata are not replaced. This applies to mx-static management, not arbitrary external Docker commands or the app's normal business writes.


- Latest server recovery receipt: independent `/etc/mx-static/nas/infra-media.json` registered, snapshot `a25a1eae157b9e99636f` installed/current, unified migrated policy enabled, infra verified/included, timer active/enabled; delta waiting for migration. No need to repeat switching or recreate currently healthy NAS consumers.
- Latest Part 1 acceptance receipt: `mx-nas-part1-reclaim-check-53504b393b.service` succeeded with plan `/var/lib/mx-static/nas-cutover/po_infra_media_data-33e5abb193d04e7595251a5e6a6046ae/reclaim-plan-a1dc4bb0dfea4d5d83ce143daaf052e9`. `files_verified=true`, `business_acceptance_recorded=true`, `recovery.verified=true`, `reclaim_ready=true`; `deletion_authorized=false`, `source_deleted=false`. 200,543 files / 538,031,658,699 logical bytes; 200,464 quick matches, 79 hashes equal, 1,681 NAS attributes retained. SSD/stopped manifest SHA256 `049f14e174ae53e42400d775605e03310ede473ac61cdd1f77fa031d3b5c099d`. Git now selects this exact accepted plan, not the earlier unaccepted `df68511205fd47c9af3be3d643455300`. Server must sync the selection and reinstall/check recovery so the installed snapshot matches. This completes the requested Part 1 readiness endpoint, with SSD retained for separately requested later deletion and live revalidation.
- Part 2 may now proceed to its independent unlimited online precopy via `nas delta copy --unlimited` after syncing/checking the final Part 1 registration. No Part 2 copy-start/completion receipt yet. Its cutover/recovery/reclaim adapter is still unimplemented; don't claim online precopy achieves the final migration endpoint or use Part 1 identities. No database/queue deletion or Part 1 cleanup is authorized by this transition.

- User now asks for the command to delete Part 1 retained SSD media after the accepted readiness receipt. Provide the guarded `nas infra cleanup --business-accepted` for the explicitly registered `a1dc4bb0dfea4d5d83ce143daaf052e9` plan, after syncing the registry and checking current recovery. No actual deletion completion is confirmed. This request does not permit NAS/database/queue deletion or skipping revalidation. Prefer project-only commands (`nas infra cleanup`, `nas delta copy --unlimited`); existing routing already resolves their registered tasks, and explicit task forms remain compatible.

## Required behavior

1. Preserve existing successful reports, NAS marker, volume IDs, paths and cleanup ledgers. Do not silently rebase identity/configuration checks, move live data, or convert a retained SSD copy back into the authority.
2. Default to read-only diagnostics. Write probes require their explicit flag and may clean up only the exact probe they created. Never recursively chmod/chown live media, use chmod 777, change NAS squash/export policy, force-unmount, switch hard NFS to soft, or restart all Docker services as a generic fix.
3. Destructive cleanup is restricted to an exact verified manifest after business acceptance, with rechecks and durable progress. Never delete a Docker volume, database/queue data or another project's data as part of media cleanup. File name/age alone (including `.tmp`) is not deletion authorization.
4. Existing user authorization remains valid: do not introduce repeated approval prompts, a cloud-backup requirement or a full SHA256 reread gate for the accepted Part 1 workflow. Required business acceptance is an operational fact, not something to assume.
5. Recovery starts only registered existing NAS consumers and their reviewed existing PostgreSQL/Redis dependencies. It must not copy old SSD over live NAS, create a local fallback, purge tasks, or force-restart running processes. Preserve independent SSD databases/queues; only stopped dependencies may be started after data-volume checks.
6. Add new projects through `deploy/nas` registrations and a reviewed adapter. A catalog entry is not authorization for a generic migration/deletion engine. Keep old CLI forms compatible while jobs may be running.
7. Root operator access does not prove application access. Check the application's container identity and actual bounded I/O, keep gateway readers read-only, and change permission policy only through a separately scoped plan.
8. mx-static's archive worker has its own object/manifest ownership. Never attach it to an existing business raw-media directory as if that directory were an initialized archive. Preserve one clearly defined writer/cleanup owner per namespace.
9. NAS placement must fail closed across reboot/reinstall: missing metadata/configuration never authorizes an SSD fallback. Read-only `infra storage check` detects current placement but does not intercept external releases. Do not claim the old deployment path is protected until a mandatory release/start control is implemented and verified. Preserve MX-H2I login/network behavior; no auth reset or database initialization as a storage repair.

## Verification and operations

Use `bash scripts/manage.sh nas` as the public interface. Keep app-specific checks in `scripts/nas/projects/`, host diagnostics in `scripts/nas/host.py`, and declarations in `deploy/nas/`. Runtime receipts/credentials are not Git artifacts.

Run meaningful NAS regression tests (`python3 -B -m unittest discover -s tests -p 'test_nas*.py'`), Bash syntax and Python 3.6 compatibility checks after safety-critical changes. Real local file tests do not prove EL8/NFS/systemd reboot behavior; state the tested boundary. Do not perform an online restart, outage drill, cleanup or installation merely to validate local code.
