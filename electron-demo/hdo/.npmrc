# Keep a real node_modules layout for packaged HDO builds. The marketplace seed
# flow and native runtime files are intentionally copied as OS-readable files
# because this app is packaged with asar disabled.
node-linker=hoisted
