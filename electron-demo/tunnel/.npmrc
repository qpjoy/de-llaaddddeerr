# Electron Forge requires a flat node_modules layout (real files, not pnpm
# store symlinks). Without this, `forge package` fails its preflight check
# and even if you bypass that, native modules + tunnel's seed install break
# because forge's resource copy doesn't follow into `.pnpm/<store>/`.
node-linker=hoisted
