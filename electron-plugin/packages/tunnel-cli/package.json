{
  "name": "@qpjoy/tunnel-cli",
  "version": "0.1.7",
  "description": "Global QPJoy Tunnel CLI for mihomo-client and cross-platform HDO mesh enrollment.",
  "private": false,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "qp-tunnel-cli": "dist/index.js"
  },
  "files": [
    "dist",
    "resources",
    "scripts",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "tsc -p tsconfig.json && node scripts/prepare-package.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@qpjoy/electron-core-wireguard": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^22.10.7"
  }
}
