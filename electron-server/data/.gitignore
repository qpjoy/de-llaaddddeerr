# All operational data lives here (regenerated by `pnpm sync:npm`).
# We commit the bootstrap version + index files so a fresh checkout has
# something to serve immediately, but every other file is local.
*
!.gitignore
!version.json
!marketplace-index.json
